
############################################### MODULE 0 : FOUNDATION ##########################################################

Kotlin is a programming language. It can be used to write many types of applications.

Types of Applications which Kotlin Support :
	│
	├── Console Application		-	A console application is any program that runs in a console/terminal.
	├── Desktop Application
	├── Android Application
	├── Web Application
	├── Server Application
	└── CLI Application			- is a special type of console application that accepts commands or arguments from the user. Example : git status


Kotlin can be used to develop many application types : 
											Kotlin
											 │
        ┌────────────────────────────────────┼────────────────────────────────────────┐
        ▼         						     ▼         							      ▼
    Desktop App   						 Android App   							  Server App
        │         						     │         							      │
        ▼         						     ▼         							      ▼
Compose Multiplatform  					Android XML (Old)  						 Ktor/Spring
(Desktop Target)       					Jetpack Compose    						 (No UI)
	│
	├── Android Target
	├── Desktop Target
	├── Web Target
	└── iOS Target
	
	
********************************* A. DESKTOP APPLICATION LIFECYCLE *********************************

--> What Is A Desktop Application?
	A desktop application is a program that runs as a process under the Operating System (such as Windows, Linux, or macOS) and provides a Graphical User Interface (GUI) through which the user interacts with the application.
	
	A desktop application is ultimately a program that the Operating System starts and manages.
	Not every desktop application must have a GUI. Console applications can also run as processes on a desktop Operating System.
	
--> Desktop Application Lifecycle
	User	->	Double Clicks EXE	->	Windows Operating System	->	Loads EXE into RAM	->	Creates Process	->	Initializes Process Resources	->	Starts Primary Thread	->	Transfers Execution to Entry Point	->	main() Executes

--> Desktop Application Startup Flow : 

	Step 1 : User Starts The Application
		The user double-clicks an executable (.exe) file.
		The double-click is received by the Windows Operating System, not by the application.
		The application is not running, No Process Exist, No Window Exist yet.

		--> What Is An Executable (.exe)?	
			An Executable (EXE) is a file that contains	:	Machine instructions	|	Program code	|	Static data	|	Information required by the operating system to load the program
			An EXE is not a running program.It is simply a file stored on secondary storage.
			
	Step 2 : Windows Loads The Executable into RAM
		Windows identifies the .exe file as an executable program.
		Windows prepares the executable and required libraries in the process's virtual address space.

		Reason:
			The CPU executes instructions through memory, while the Operating System manages how program code and data are mapped into the process's address space.

	Step 3 : Windows Creates A Process
		An executable (.exe) is only a file stored on disk. It is NOT a running program.
		
		When Windows starts the executable, it creates a Process.
			Think:
				EXE       -> Blueprint
				Process   -> Running Program
		
		A Process is a running instance of a program. During process creation Windows initializes :  Program Code | Stack | Heap | Global Variables | Primary Thread | Handles | Environment
		These are runtime resources associated with the process. They are not already-created runtime objects stored inside the EXE.
		They are created only when the process starts.
	
	Step 4 : Windows Transfers Control To The Entry Point
		After the process is initialized, execution eventually reaches the program's entry point.
		For a Kotlin/JVM Desktop application, execution eventually reaches:
			fun main(args: Array<String>)
			
		At this stage:

			✓ Process is running.
			✓ main() is executing.
			* window not exist

		However, No graphical window exists yet.

		Reason : The program has not requested the operating system to create a window.
		
==> SUMMARY OF CHAPTER 1 :
		User		->		Starts EXE		->		Windows Operating System		->		Loads EXE into RAM		->		Creates Process		->		
																																					|
																																					|
																																					↓						
		No Window Exists Yet		<-		main() Starts Executing		<-		Transfers Execution to Entry Point		<-		Initializes[Program Code / Stack / Heap / Global Variables / Primary Thread / Handles / Environment Information]
						
			
********************************* B. WINDOWING SYSTEM *********************************

--> What Is A Windowing System?
		It is a part of the Operating System for creating and managing graphical windows.
		
		Responsible  : Creating Window | Showing Window | Moving Window | Resizing Window | Closing Window | Mouse Events | Keyboard Events

--> Does Every Process Have A Window?	-   NO.

    Reason : Not every application requires a GUI.
		Examples : Git | Compiler | Ping | CMake

    Rule : A process does NOT automatically get a Window.The application must request the Operating System/windowing system to create one.

--> Who Creates The Window?
		Application does NOT create the Window.
		
		Flow : Application -> Windows API -> Windowing System -> Native Window Created
			
--> Windows API
		The Operating System exposes functions that applications use to communicate with Windows.
			Examples : CreateWindow() | DestroyWindow() | ShowWindow() | MoveWindow() 
			
		Think : Windows API = Interface through which applications/frameworks request Operating System functionality.
		
--> Native Window
	The actual desktop window is an OS-managed native window.
	The application/framework requests the Operating System to create it and can then interact with it through platform APIs.

--> Window Handle (HWND)
		Windows identifies a native window using a handle.
		It is commonly represented by HWND in Win32 programming.

		Applications use this handle whenever they want Windows to perform another operation.
		Example : HWND	->	MoveWindow()
		
		Think :  HWND = Window ID / Window Reference

--> Why Doesn't Windows Return The Actual Window?
	Applications interact with the native window through an OS-defined handle/API instead of directly manipulating the Operating System's internal window object.

	This protects the Operating System's internal implementation and prevents applications from directly modifying its internal structures.


--> Event Driven Programming
	Console programs execute sequentially.
		Example Console Application Works : Step 1 -> Step 2 -> Step 3 -> Exit

    Desktop Application : User interaction can determine when different actions occur.
		Example : Click Button | Press Key | Resize Window | Move Window


--> What Is An Event?
	An Event is an action or occurrence that the application can respond to while it is running.
		Examples	:	Mouse Click		|	Mouse Move

		
--> Message Queue
		Windows receives hardware/input events.
		Examples : Mouse Click | Keyboard Press | Window Resize

    Windows delivers relevant events to applications through messages/events.
	These messages can be queued and processed by the application.

--> Message Flow
		User Clicks Mouse -> Windows OS Detects Event -> Creates/Dispatches Message -> Application Receives Message -> Processes Message

--> Message Loop
	Desktop applications need to continue running and process incoming events after creating a window.
	A traditional Windows desktop application uses a Message Loop to continuously retrieve and process messages.



********************************* FLOW OF CHAPTER 1 AND 2 *********************************

User Double Clicks EXE  ->  Windows Operating System  ->  Loads EXE into RAM  ->  Creates Process  ->  Initializes Process Resources -> Starts  ->  Calls main()
																																				│
																																				│
																																				│
																																				│
		Returns Window Handle (HWND)  <-	Creates Native Window  <-	Windowing System  <-	Windows API  <-	Application requests Window	 <--│
		│
		│
		│
		│																──────── Runtime ────────
		│-->  Application/UI Framework Processes User Interaction ->  User clicks mouse  ->  Windows receives event  ->  Creates/Dispatches Message -> Application/Framework Receives Message
																																								│
																																								│
																																								│
																																								│
																								Application processes Message <-  Application reads Message  <--│


********************************* C. WHY DO WE NEED A UI FRAMEWORK? *********************************

--> Why WE need Compose(framework) if Windows(OS) already provides API like : CreateWindow(), ShowWindow(), MoveWindow() becuase We can directly use that API into our Kotlin Application 
		
		1. A UI Framework does not replace the Operating System. It provides a higher-level abstraction over low-level APIs.
		2. Windows already provides the fundamental capabilities. Compose provides a much easier and more organized programming model for using those capabilities.
		3. A UI Framework reduces the amount of platform-specific and repetitive code developers need to write.

		Example without Compose :	Create Button | Set Width  | Set Height | Set Font | Set X Position | Set Y Position		Lots of Works
		
		Example of Compose 		:	
					Button(	
						modifier = Modifier.padding(16.dp)
					)
			Compose handles many UI concerns such as : Layout | Measurement | Drawing | Input/Event Handling | UI Updates
	
			Compose does not replace the Operating System.
			For a desktop application, the framework ultimately integrates with the platform's native windowing system.

		
--> Compose was created because writing directly with low-level APIs becomes:
		Too much code	|	Difficult to maintain	|	Difficult to adapt to different screen sizes	|	Difficult to reuse UI components
		
--> Can Kotlin Create UI?		-	   NO.
		Kotlin is only a programming language, not a UI toolkit.
		
		Kotlin provides 				:  Variables | Functions | Classes | Coroutines | Collections 
		It does NOT itself provide		:  Window |  Button | TextField | Image | Row | Column

		To create Desktop UI, Kotlin can use: : Compose Desktop / JavaFX / Swing

--> Can C++ Create UI?		-	   NO.
	
	C++ is a programming language.
	C++ applications also require a UI technology/framework or platform API.

	Examples	:	Win32 API | MFC | Qt | wxWidgets
		
--> Problems with Win32 API
		Win32 API is a low-level API. It provides maximum control but requires a large amount of manual work.
		Every Windows desktop application ultimately depends on it (directly or indirectly).
		
		We have to manually : Create Window | Create Label | Create TextBox | Create Password Box
		
		Main Problem :  Too much Boilerplate Code. | Manual X,Y Positioning | Difficult Maintenance. |  Manual Message Handling. | Platform Dependent (Windows Only)
		
--> Why Do We Need A UI Framework?
		Instead of working directly with low-level APIs, developers use a UI Framework.
		
		Instead of writing low-level code, it provides high-level components and abstractions.
			Examples :  Window |   Button |  Text |   TextField |  Image |  Menu
			
--> Responsibilities Of UI Framework
	A UI Framework provides high-level components and hides platform-specific implementation.

	Responsibilities : Create UI Components.  | Manage Layout | Handle Events |  Draw UI |  Manage UI State | Hide Platform Specific APIs
	
--> Does A UI Framework Replace The Operating System?		-	No
	The Operating System still provides : Native Windows	|	Mouse Events	|	Keyboard Events
	The framework communicates with the Operating System and provides a higher-level API to the application developer.

********************************* D. EVOLUTION OF DESKTOP UI *********************************
--> Desktop UI Architecture

        Application		->		UI Framework		->		Native API		->		Operating System


Layer 1 - Operating System
	
	Responsibilities:
		The OS actually creates : Create Window | Move Window | Resize Window | Destroy Window |  Mouse Events | Keyboard Events | Message Queue

Layer 2 - Native API

    Native API is provided by the Operating System.

    Purpose :  Allows applications/frameworks to communicate with the Operating System.

    Example (Windows):  Win32 API
	
	Example Functions : CreateWindow() |  ShowWindow() | DestroyWindow()


Layer 3 - UI Framework
	 
	UI Framework sits above Native API. 
	Frameworks like : MFC  |  Qt  |  Swing  |  JavaFX  |  Jetpack Compose	|	Compose Multiplatform
	
	Responsibilities :	Reduce Boilerplate Code		|	Provide Ready-Made UI Components	|        Handle Layout	|	Handle Events	|	Improve Maintainability		|		Hide Native API Complexity.
	
--> Evolution

        Win32 API (Native API)
              │
              ▼
        MFC (C++ Framework)
              │
              ▼
        Swing (Java Framework)
              │
              ▼
        JavaFX (Modern Java Framework)
              │
              ▼
        Android XML
              │
              ▼
        Jetpack Compose
              │
              ▼
        Compose Multiplatform
	
--> Win32 API

    • Native Windows API.	|	• Provided by Microsoft.		|	• Used to communicate with Windows OS.	|	• Low-Level Programming.
    • Maximum Control.		|	• Too much Boilerplate Code.	|	• Manual UI Management.
	
--> MFC (Microsoft Foundation Classes)

    • C++ UI Framework by Microsoft.	|	• Object-Oriented Wrapper over Win32 API.	|	• Reduces Boilerplate Code.	|	• Internally uses Win32 API.
	
	Flow : 	Application	->	MFC		->	Win32 API		->	Windows OS
	
--> Swing

    • Java UI Framework.			|	• Platform Independent.					|	• Write Once, Run Anywhere.			 |	• Internally communicates with Native APIs through JVM.
    • On Windows → Uses Win32 API.	|	• On Linux → Uses Linux Native APIs.	|	• On macOS → Uses macOS Native APIs.

    Flow (Windows):   Application	->	Swing	->	JVM		->		Win32 API		->	Windows OS
	
--> JavaFX

    • Modern Java UI Framework.		|	• Replacement for Swing.		|	• Better UI.		|	• Better Animation.
    • Better Styling.				|	• CSS Support.					|	• FXML Support.

    JavaFX also communicates with Native APIs through the JVM.
		
--> Android XML		
	
	Developed by Google for Android applications. It is NOT used in Desktop Applications.
	XML is a Markup Language, not a programming language.
	Example:
        activity_login.xml		->	  XML File 				=>  UI Design
                +
        LoginActivity.kt		->	 Kotlin / Java File		=> Business Logic
   
	XML Purpose : Used to describe UI.
		Example : Button | TextView | EditText | ImageView
		
		
	Responsibilities :
	
		XML					:	UI Design	  |	Screen Layout	|	UI Components
		
		Kotlin / Java		:	Click Events  | Validation  	|  API Calls  |  Database  |  Navigation  |  Business Logic	
	
	Advantages : 
			• Separation of UI and Logic. | • Easy for Designers. | • Better Organization (Initially).
			
	Problems : 
			• Two files for one screen.  |  • Constant switching between XML and Kotlin.  |  • Difficult to maintain large projects.
			• UI updates require communication between XML and Kotlin.  |  • More Boilerplate Code.
			
--> Jetpack Compose

	Developed by Google .Replace Android XML with Jetpack Compose .

	Jetpack Compose is a modern UI Toolkit developed by Google for building Android user interfaces using Kotlin instead of XML.
	
	Instead of writing UI in XML, developers describe UI directly using Kotlin.
	
	Advantages of Compose :
		One Language	|	Less Boilerplate |  Easier Maintenance  |  Modern UI Development[Declarative UI]
		
		
--> Compose Multiplatform

	Developed and maintained by JetBrains as a multiplatform UI framework based on the Compose programming model.
	Compose Multiplatform allows developers to share Compose UI code across supported platforms.

	Targets:
		Android
		Desktop (Windows, Linux, macOS)
		iOS
		Web (Experimental)

	Current Project :    Desktop Target (Windows)


------------------------------------------------------------------------------------------------------------


####################################### Module 1: What Compose Multiplatform is and how Compose works conceptually ##########################################################

********************************* A. WHAT EXACTLY IS COMPOSE MULTIPLATFORM *********************************

What is a Framework? 	-	A framework is a collection of pre-written code, libraries, and rules that helps you build applications faster.
	Examples: Kotlin
				│
				├── Jetpack Compose (Android UI Framework)
				├── Compose Multiplatform (Cross-platform UI Framework)
				├── Ktor (Backend Framework)
				└── Spring Boot (Backend Framework for Java/Kotlin)

--> What is Compose Multiplatform
	
	Compose Multiplatform is a modern declarative UI framework developed by JetBrains.
	It allows developers to build user interfaces using Kotlin and share UI code across multiple supported platforms.
	Instead of creating completely separate UI implementations for each platform, we can share a large portion of the UI code.

	Conceptually:

		Kotlin + Compose Multiplatform
					↓
			Shared UI Code
					↓
			┌─────────┼─────────┐
			↓         ↓         ↓
		Android    Desktop     iOS
			↓         ↓         ↓
		Platform-specific implementation underneath
	
	Compose Multiplatform does NOT mean that every platform internally works exactly the same way.
	The goal is to share UI/application code while Compose and the platform integration handle the differences underneath.

		
--> What Is A UI Toolkit?
	A UI Toolkit is a collection of APIs and components used to build graphical user interfaces.

	It usually provides things such as : Text | Button | TextField | Image | Layout | Input Handling | Styling | Rendering

	Example : Compose	|	JavaFX	|	Swing	|	Qt

	Think : UI Toolkit = Tools used to build UI.
	
	
--> UI Toolkit vs UI Framework
	
	A UI Toolkit mainly provides reusable UI components.
	A UI Framework provides : UI Components | Architecture | Rules | Runtime | State Management | Event Handling | Layout System

	Compose Multiplatform provides all of these.


--> Compose Multiplatform vs Jetpack Compose

	Jetpack Compose:

		• Developed by Google.	|	• Primarily designed for Android UI.	|	• Uses Kotlin.	|	• Provides a declarative UI programming model.

	Compose Multiplatform:

		• Developed and maintained by JetBrains.	|	• Builds on the Compose programming model.	|	• Extends Compose UI development to multiple platforms.
		• Uses Kotlin.	|	• Allows UI code to be shared across supported platforms.

	Think:
		Jetpack Compose			->			Android-focused

		Compose Multiplatform	->			Multiplatform UI

--> Why JetBrains created Compose Multiplatform?

	Developers often want to build applications for multiple platforms without writing the entire UI separately for each platform.

	Without shared UI:
		Android UI	->	Separate Code
		Desktop UI	->	Separate Code
		iOS UI		->	Separate Code

	With Compose Multiplatform:
		Shared Compose UI	->		Multiple Platforms

--> Compose Multiplatform Targets

	Compose Multiplatform supports UI development for multiple platforms.
	Common targets include : Android	|	Desktop	|	iOS		|	Web
	
--> Desktop Target
	Compose Multiplatform can be used to create desktop applications.

	Supported desktop operating systems include : 	Windows | Linux | macOS

	Our application will primarily use : Compose Multiplatform	->	JVM/Desktop	->	Windows

--> Android Target
	Compose Multiplatform can share UI code with Android applications.

	Conceptually:
		Shared Compose UI	->	Android Platform	->	Android Application


--> Shared UI Concept
	Shared UI means that the same Compose UI code can be used across multiple platforms.

	Example:

		@Composable
		fun LoginScreen() {
			Column {
				Text("Login")
				Button(
					onClick = { }
				) {
					Text("Login")
				}
			}
		}

	The same UI function can potentially be used by multiple supported platforms.

	Conceptually:
		LoginScreen()	->		Shared Compose UI	->		Android / Desktop / iOS


	Important:
		Shared does NOT mean that every single line of application code must be shared.
		Platform-specific code may still be required when interacting with platform-specific APIs.

--> Platform-specific UI Concept
	Some functionality depends on the platform.

	Example:
		Windows-specific API	|	Android-specific API	|	iOS-specific API

	In such cases we can keep the platform-specific implementation separate.

	Conceptually:
		Shared Code
			│
			├── Common UI
			│
			└── Platform-specific Code
					├── Android
					├── Desktop
					└── iOS

	Example:
		Shared:
			LoginScreen
			HomeScreen
			Button
			Text
			Layout

		Platform-specific:
			Windows API
			Android API
			iOS API
			
--> Declarative UI	vs Imperative UI

	Declarative means : 
		Describe what the UI should look like.
			Example : Text("Hello")
	
		We are simply describing the UI.Compose decides : Size | Drawing | Rendering | Updating
		
		Flow : State Changes	->	Compose Detects Change	->	Recomposition	->	Updated UI
		
	Imperative means : 
		Imperative UI means we explicitly tell the UI system HOW to perform operations.
		Example : Suppose I want a Login Button
			So we explained every step to Framework like : Create Window -> Create Button -> Set Width -> Set Height -> Set Color -> Set Position -> Draw Button
		
		Exampele : User Clickk		->		Increase countk		->		Find TextViewk		->		Convert Int → Stringk		->		Set Textk		->		Refresh UI
	
	--> Why Did Google Choose Declarative UI?
		Modern applications constantly change.Updating every UI component manually becomes difficult.
		Frameworks like Compose automatically update the UI whenever the application's State changes.his makes applications : Easier to write | Easier to maintain

--> Why Is Compose Called Declarative?
		Compose follows the Declarative UI approach.The developer describes what the UI should look like.Compose decides how to create and update that UI.


************************************************** B. COMPOSE APPLICATION STRUCTURE ***********************************************

--> application()
	application() is a Function provided by Compose Multiplatform.
	It is the Entry Point into the Compose Framework.
	Example :
		fun main() = application {
		}
	This is equivalent to :
		fun main(): Unit {
			return application {
			}
		}
	
	When Windows starts our application : 	
		Windows OS ->	Process	->	JVM	->	main() -> application()
		
		At the point main() starts: 
			✓ Process exists
			✓ JVM is running 
			✓ main() is executing 
			✗ Compose Desktop UI has not been created yet 
			✗ Native application Window has not been created yet
	
	So Who is responsible for starting the Compose environment?			-	Compose Multiplatform	
	It cannot be : Windows OS | Kotlin | JVM
		
		
	1 -  
		fun main(args: Array<String>)
		Meaning:
			Function Name → main	|	Parameter → args	|	Return Type → Unit (implicit)
		Equivalent to :
			fun main(args: Array<String>): Unit
	
	2 — Expression Body (=)
		means:
			fun main(args: Array<String>): Unit {
				return application {

				}
			}
		because in Kotlin :
			fun add(a: Int, b: Int): Int {
				return a + b
			}
			fun add(a: Int, b: Int): Int = a + b						//Expression Body Functions

		
	3 — Why application { } ?
		Conceptually :
			fun application(
				content: () -> Unit
			)

		application() accepts a Function as its parameter.
		So application() is a Higher Order Function.
	
	5 — Trailing Lambda
		application {
			// Your code				//The lambda[code inside {} ] contains the code that Compose will execute AFTER it initializes the Compose Runtime.
		}

	Responsibility : 
		application()
			↓
		Initialize Compose Runtime
			↓
		Prepare Compose Environment
			↓
		Execute the Lambda

--> Role of application()
	application() provides the Compose Desktop application environment and manages the desktop application's lifecycle.

	Inside application(), we can create one or more Compose Windows.

	Example :
		fun main() = application {

			Window(
				onCloseRequest = ::exitApplication
			) {

				App()

			}
		}


	Conceptually :
		main()
		  ↓
		application()
		  ↓
		Window()
		  ↓
		Compose UI
	
	application() is also a Higher-Order Function because it accepts a lambda containing the application's Compose code.
	Example : 
		application { 
			// Compose Desktop application code 
		} 
	The code inside { } is the lambda passed to application().
	
--> Window()
	Window() is a function provided by Compose Multiplatform for creating and configuring a Desktop Window.
	It is also a Higher Order Function because its LAST parameter is a Lambda.
	
	Example :
		Window(
			onCloseRequest = app::onDestroy,
			title = "ATEK GATE",
			state = rememberWindowState(
				placement = WindowPlacement.Fullscreen
			)
		) {
			App(app)
		}
	
		Window() also accepts a lambda as its content. 
		The lambda describes the Compose UI that should be displayed inside the Window.
		
	--> Role of Window()
		Window() connects the Compose application with the platform's Desktop Windowing System.

		Conceptually :
			Window()
				↓
			Platform Windowing System
				↓
			Operating System
				↓
			Native Desktop Window

		Important :
			Compose does not replace the Operating System's Windowing System.
			The actual native Desktop Window is ultimately created and managed through the platform's windowing system.
			Window() provides the Compose API through which our application configures and hosts UI in that Desktop Window.

		Example :
			Window {
				App()
			}

		Meaning :
			Create/configure a Desktop Window
					+
			Display App() as the Compose content inside it.
		
	--> Why Does Window() Accept a Lambda?
		Window() needs to know what Compose UI should be displayed inside the Window.

		So we provide the UI as a lambda.

		Example :
			Window {
				App()
			}

		The lambda:
			{
				App()
			}

		describes the Compose content of the Window.
		
		Think : 
			Window() -> Where the UI is displayed 
			Lambda -> What UI is displayed


--> Scene()
	Scene is an internal architectural concept used by Compose Desktop.
	After the Desktop Window is created, Compose needs an environment in which it can manage and render the Compose UI associated with that Window. 
	This Compose-managed environment is referred to as a Scene.
	
	Conceptually : Native Desktop Window -> Compose Scene -> Compose UI -> App() -> Composable Functions
	
	EXAMPLE: 
		+-----------------+
		| ATEK GATE  X    |
		+-----------------+
		| +-----------+   |
		| |           |   |
		| |    Scene  |   |
		| +-----------+   |
		+-----------------+
 
	--> What lives inside the Scene?
		App()	|	MainScreen()	|	Buttons() |  Text()  |  Images
		
	--> Why do we need Scene?
		The Native Window is responsible for the platform-level Window.
		Compose is responsible for managing and rendering the Compose UI.
		The Scene provides the connection/environment between these two layers.
		
--> Relationship between application(), Window(), Scene and UI

	Complete structure :

		fun main() = application {
			Window(
				onCloseRequest = ::exitApplication
			) {
				App()
			}
		}


	Conceptual flow :
		main()	->	application()	->	Window()	->	Native Desktop Window	->		Compose Scene	->	App()	->	Composable UI


	Each part has a different responsibility :

		main()					→ Application Entry Point
		application()			→ Compose Desktop Application Environment / Lifecycle
		Window()				→ Desktop Window Configuration and Compose Content Host
		Scene					→ Compose-managed environment connecting the native window with Compose UI
		App()					→ Application's UI Root
		Composable Functions	→ Describe individual pieces of UI


********************************* C. COMPOSE MENTAL MODEL *********************************			

--> What is a Composable?
	A Composable is a function that participates in Compose's UI composition system.
	It describes a piece of UI or UI-related behavior.

	Example:
		@Composable
		fun Greeting() {
			Text("Hello")
		}


	Think : Composable = A building block of Compose UI.


--> Composable Function
	A Composable Function is a Kotlin function marked with @Composable.

	Example: 
		@Composable
		fun MainScreen() {
		Text("Welcome")
		Button( onClick = { } )
			{ 
				Text("Login")
			} 
		}

	A Composable function can call other composable functions.

	Example:
		@Composable 
		fun MainScreen() { 
			Header()
			Body() 
			Footer()
		}
	
	Responsibility :  Describe a reusable piece of UI that can participate in Compose Composition.

--> @Composable — basic meaning
	@Composable is an annotation used by Compose to identify functions that participate in the Compose composition model.

	Example:
		@Composable
		fun App() {
			Text("Hello")
		}


	Important:
		@Composable is not simply a normal annotation that means "this function is UI."
		The Compose compiler/plugin processes composable functions so that they can work with the Compose Runtime.

	Basic Flow:
		@Composable
				↓
		Compose Compiler
				↓
		Compose Runtime
				↓
		Composition / Recomposition


	Important Rules:
		A Composable Function can call another Composable Function.
		A normal Kotlin Function cannot directly call a Composable Function because it does not have a Compose composition context.
		
--> Composition
	Composition is the process in which Compose executes composable functions and builds the current UI structure described by them.

	Example:
		@Composable
		fun App() {
			Column {
				Text("Hello")
				Button(
					onClick = { }
				) {
					Text("Click")
				}
			}
		}

	Composition means:
		Building and managing the UI structure from Composable Functions.

	Important:
		Composition is NOT the same as drawing pixels on the screen.
		Composition determines and manages the structure of the Compose UI.
		Measurement, layout, and drawing are separate stages of rendering.


--> Composition Tree
	The Composition Tree represents the hierarchical structure created by composable functions during composition.

	Example:
		@Composable
		fun App() {
			Column {
				Text("Hello")
				Row {
					Text("Name")
					Button(
						onClick = { }
					) {
						Text("Save")
					}
				}
			}
		}

	Conceptually:
		App
		 ↓
		Column
		 ├── Text("Hello")
		 │
		 └── Row
			  ├── Text("Name")
			  │
			  └── Button
				   └── Text("Save")

	Think:
		Composable Functions
				↓
		Composition
				↓
		Composition Tree
		
	The Composition Tree allows Compose to keep track of the structure and relationships involved in the UI.

--> State
	State is data whose current value can change during the lifetime of the application and whose change can affect the UI.

	Example:
		count = 0				//Previous State

	After an action:	
		count = 1				//New State



	In Compose, observable UI state can be created using : mutableStateOf()

	Example:
		var count by remember {
			mutableStateOf(0)
		}


	Think:
		State = Current data that can affect the UI.

--> UI as a Function of State

	The fundamental Compose idea is:
		UI = f(State)

	Meaning:
		The UI shown by the application depends on the current State.

	Example:

		var count by remember {
			mutableStateOf(0)
		}

		Text("Count = $count")


	When:
		count = 0
		UI:
			Count = 0


		count = 1
		UI:

			Count = 1


	Think:

		State Changes
				↓
		Compose observes change
				↓
		Relevant UI is recomposed
				↓
		UI reflects new state

--> Relationship between State and UI

	State ≠ UI

	State	→	Data
	UI		→	Visual representation based on current State

	The developer changes the State.
	Compose observes the relevant state and updates the UI representation when necessary.
	
--> Recomposition
	Recomposition is the process where Compose re-executes relevant composable functions when observed state changes.

	Example:

		@Composable
		fun Counter() {

			var count by remember {
				mutableStateOf(0)
			}

			Button(
				onClick = {
					count++
				}
			) {
				Text("Count = $count")
			}
		}


	Initial:

		count = 0
		↓
		Text("Count = 0")


	User clicks Button:

		count++
		↓
		count = 1
		↓
		Recomposition
		↓
		Text("Count = 1")


	Important:

		Recomposition does NOT mean that the entire application is destroyed and rebuilt from zero.
		Compose uses information about the existing composition to determine which parts need to be re-executed.

--> How Compose builds UI

	At a simplified conceptual level:

		1. Application starts.
		2. Compose starts its runtime/composition.
		3. Root composable is executed.
		4. Root composable calls child composables.
		5. Compose records/manages the composition.
		6. UI is rendered on the screen.


	Example:

		App()
		 ↓
		LoginScreen()
		 ↓
		Column()
		 ├── Text()
		 └── Button()


	Compose uses this composable structure to manage the UI.

--> How Compose updates UI

	At a simplified conceptual level:

		State Changes 
			↓
		Compose detects the State change 
			↓
		Relevant Composable Functions are scheduled for Recomposition
			↓ 
		Composable Functions execute again as needed 
			↓ 
		Compose determines the required UI changes 
			↓ 
		Layout / Rendering is updated 
			↓
		UI reflects the new State


	Example:

		count = 0
				↓
		Text("Count = 0")

		User clicks
				↓
		count = 1
				↓
		Recomposition
				↓
		Text("Count = 1")
	
	Important: 
		"Only affected Composables execute again" is a useful simplified mental model. 
		In reality, Compose uses the Composition and compiler/runtime information to determine the scope of recomposition, and the exact amount of code re-executed can depend on the composition structure and state reads.

--> Compose Runtime — basic concept

	Compose Runtime is the underlying runtime system that manages the composition and state-driven UI updates.

	It provides the mechanisms needed for concepts such as:
		Composition	|	State Observation	|	Recomposition	|	Remembered Values	|	Composition Management


	Think:

		Composable Functions 
			↓
		Compose Compiler 
			↓
		Compose Runtime
			↓
		Composition 
			↓
		State Changes
			↓
		Recomposition 
			↓ 
		UI Update


	Important:

		Compose Runtime is not the same thing as the UI components themselves.

		Runtime manages the composition model.

		UI libraries provide components such as : Text | Button | Column | Row | Box

			
************************************************** Desktop Application Startup  Flow ************************************************** 

	Windows OS		-->		Loads EXE		-->		Creates Process		-->		Starts JVM		-->		JVM Executes main()		-->		application()		-->		Compose Runtime Initializes		-->		application()
																																																				|
																																																				|
																																																				↓
	Composition		<--		Scene Created		<--		Windows OS creates Native Desktop Window		<--		Window()		<--		application() executes its Lambda		<--		Compose Runtime Starts
		|
		|
		↓
	Composable Functions Execute		-->		UI Rendered		-->		User Interaction		-->		State Changes		-->		Compose Detects State Change
																																		|
																																		|
																																		↓
										Updated UI		<--		Only Affected Composable Functions Execute Again		<--		Recomposition	
	
------------------------------------------------------------------------------------------------------------


####################################### Module 2: How to write and configure basic Compose code. ##########################################################

********************************* A. COMPOSE SYNTAX & ANNOTATIONS *********************************
	
--> What is an Annotation?	
	An annotation is metadata[Data about Data].
	It provides extra information about a program element.	It does NOT change the actual code.
	
		Example :
		
			@Composable
			fun App() {
			}
			The function is still a normal Kotlin function.	
			The annotation only provides additional information.
	
	--> Why do we need Annotations?

		Without Annotations, the Compiler or Framework would not know that something requires special processing.
		Annotations allow us to mark code with additional information.
	
	--> Who Reads The Annotation?
		• Kotlin Compiler	|	• Compose Compiler Plugin	|	• Compose Runtime	|	• IntelliJ IDEA		|	• Android Studio	|	• Libraries
		
	--> Is Annotation Executable Code?
		NO.
		An Annotation is NOT executable code.
		It is only additional information attached to a program element.
		
	--> Is Annotation A Kotlin Feature?
		YES.
		Annotations are a built-in Kotlin language feature.
		
		We can even create our own Annotation.
		Example :
			annotation class MyAnnotation

		Usage :
			@MyAnnotation
			fun greet() {

			}
	--> Common Kotlin / Compose Annotations

	Examples :
		@Composable	|		@Preview	|		@Deprecated	|		@Suppress	|		@Test

	Every Annotation has a specific purpose.		

	
--> @Composable
	`@Composable` is an annotation used to mark a function as a Composable function.
	Example :
		@Composable
		fun Greeting() {
			Text("Hello")
		}
	The function is still a normal Kotlin Function, but @Composable makes it participate in the Compose composition model.
	
	 The Annotation tells Compose :
        "This Function participates in the Compose UI system  and can be used during Composition."

	--> Why do we need @Composable?
		Without @Composable, Compose treats the Function as a normal Kotlin Function.It cannot participate in Composition.
		Compose does NOT recognize it as UI.

	--> What happens after adding @Composable?
		 The Compose Compiler Plugin processes Composable functions as part of the Compose compilation process.
		Flow :
			@Composable	->	Compose Compiler Plugin	->	Transforms the Function	->	Kotlin Compiler	->	Bytecode (.class)	->	JVM Executes	->	Compose Runtime uses it during Composition
	
	--> Is @Composable executable code?
		NO.
		`@Composable` itself is an Annotation. It does not contain the UI implementation.
		It provides information that allows the Compose compiler and Compose runtime system to treat the function as a Composable.
	
	--> Who processes @Composable?

    The Compose compiler/plugin processes Composable functions during compilation.

    The Compose runtime then manages their participation in Composition and Recomposition.

    Conceptually :
        @Composable	->	Compose Compiler	->	 Compose Runtime	->	 Composition / Recomposition
	
	Important rules:
		A Composable function can call another Composable function.
		A normal Kotlin function cannot directly call a Composable function.
		@Composable is read by the Compose compiler/plugin to treat the function as part of the Compose UI system.
	
	--> Composable Function Parameters

		A Composable function is still a Kotlin function, so it can have normal Kotlin function parameters.

		Example :
			@Composable
			fun Greeting(name: String) {
				Text("Hello $name")
			}

		Calling :
			Greeting("Rahul")
			
	--> Multiple Parameters

		Example :

			@Composable
			fun UserInfo(
				name: String,
				age: Int
			) {
				Text("$name is $age years old")
			}

		Calling :
			UserInfo(
				name = "Rahul",
				age = 28
			)

		Parameter syntax is normal Kotlin syntax.
	
	--> Reusable Composable Functions

    A Composable function can represent a small, meaningful, reusable part of the UI.

    Example :
        @Composable
        fun LoginButton() {
            Button(
                onClick = {}
            ) {
                Text("Login")
            }
        }

    We can call :
        LoginButton()

    We can also make the Composable configurable.

    Example :
        @Composable
        fun LoginButton(text: String) {
            Button(
                onClick = {}
            ) {
                Text(text)
            }
        }

    Then :
        LoginButton("Login")
        LoginButton("Sign In")
        LoginButton("Continue")
		
		
--> @Preview
	`@Preview` is an Annotation used by supported Compose development tools to display a preview of a Composable.
	It tells the IDE to render a preview of a Composable without running the entire application.
	@Preview is added when we want a development-time preview.
	
	Example :
		@Preview
		@Composable
		fun LoginScreen() {

		}


********************************* B. MODIFIER *********************************

--> Modifier
	A `Modifier` is an object used to describe changes to a Composable's appearance, layout, position, interaction, or behavior.
	It does NOT create UI.
	It modifies existing UI.
	
	Example : 
		 Text(
			text = "Hello",
			modifier = Modifier
				.padding(16.dp)
		)
	
	Meaning :
		Create Button
				↓
		Apply Modifier
		We can use it like :
			Modifier
				.padding(...)					//Add padding.
				.background(...)				//Draw a background.
				.fillMaxSize()					//Occupy all available space.
				
	--> Why Modifier?

	Instead of giving every UI Component dozens of parameters,
	Compose groups common UI modifications into one object called Modifier.
	
	--> Modifier is commonly passed to a Composable through a modifier parameter
		Here:
			modifier = Modifier.padding(16.dp)

	--> Reusable Composable

		When creating our own Composable, it is a good practice to expose a `modifier` parameter when the caller may need to control its layout or appearance.

		Example:
			@Composable
			fun UserName(
				name: String,
				modifier: Modifier = Modifier
			) {
				Text(
					text = name,
					modifier = modifier
				)
			}

		Now the caller can decide how the Composable should be modified.

		Example:
			UserName(
				name = "Rahul",
				modifier = Modifier.padding(16.dp)
			)
	--> Method Chaining
		Each function returns another Modifier, allowing the next function to be chained.
	
		Example :
			Modifier
				.padding(...)
				.background(...)
				.fillMaxSize()
				
		Conceptually :
			Modifier means Start with an empty Modifier.
			Then :  
				Modifier.padding(16.dp)	
					means : Create a new Modifier that includes padding.
			Then:
				Modifier
					.padding(16.dp)
					.fillMaxSize()
					
					means Take the previous Modifier and add another modification.
					
	--> Modifier Order
		The order in which Modifier operations are written can affect the resulting UI.

		Example:
			Modifier
				.padding(16.dp)
				.background(Color.Blue)

		is not always equivalent to:

			Modifier
				.background(Color.Blue)
				.padding(16.dp)
				
********************************* C. THEME *********************************				

--> Material Components
	Compose provides ready-made UI components such as:
		• Button	|	  • TextField	|    • Checkbox	|	 • RadioButton	|	 • Card	|	 • TopAppBar	|	• Dialog

	These components follow the Material Design system.

	Instead of building a Button completely from scratch, we can use:

		Button(
			onClick = {}
		) {
			Text("Login")
		}

	So:

		Material Components
			  ↓
		Ready-made UI components
			  ↓
		Faster UI development
			  +
		Consistent design

--> Material Design
	Material Design is a design system that defines common principles for UI appearance and behavior.

	It includes things such as:
		• Colors	|	 • Typography	|	• Shapes	|	• Component design	|	• Interaction behavior

	Compose Material components use these design concepts.	
	
--> MaterialTheme

	`MaterialTheme` provides theme information to the Composables inside it.
	It provides a common UI style for all Composables inside it.
	`MaterialTheme` is not required for Compose itself to work.Compose can create UI without MaterialTheme.However, if we are using Material components, MaterialTheme is commonly used to provide a consistent design.
	
	Example:
		@Composable
		fun App() {
			MaterialTheme {
				LoginScreen()
			}
		}
	--> Purpose
		Apply a common Theme to the application.
		Examples :	• Colors	|	• Typography (Fonts)	|	• Shapes
		
	--> Example
			MaterialTheme {
				Button(
					onClick = {}
				) {
					Text("Login")
				}
			}

		The Button and Text are inside the MaterialTheme, so Material components can use the theme's values.
				
	--> Why MaterialTheme?
		Without MaterialTheme, every UI Component would need its own styling.
		Example :
			Button 1 → Color
			Button 2 → Color
			Button 3 → Color
		Instead, define the Theme once and reuse it everywhere.
			

********************************* D. STATE BASICS *********************************		

NOTE :
	Compose follows Declarative UI.

		The Developer changes the State.
					↓
		Compose automatically updates the UI.

	State Management in Compose mainly consists of :
		• mutableStateOf()	|	• remember()|	• by (Property Delegation)

	These work together to manage and update UI State.	

--> mutableStateOf()				- it notify when something change then Recomposition happen and compose get new value
	
	Creates a State Object with an initial value.
	`mutableStateOf()` creates an observable Compose State object containing a value.

	Example:
		val count = mutableStateOf(0)
			 ↓						↓
    State object				Initial value		 
				
	--> Purpose
		Create a State Object that can notify Compose when its value changes.		.
		A normal Int is just a value and cannot notify Compose.

	--> Why not use a normal variable?
		
		Normal Variable :
			var count = 0
			count++
		Compose does not automatically observe an ordinary variable as Compose State.

		Therefore, changing it does not by itself tell Compose:
			"The UI needs to be reconsidered."
		
	--> With Compose State:

		val count = mutableStateOf(0)

		count.value++					//The state object's value changes.
		
		Compose can observe this state when it is read by Composable code and schedule the relevant recomposition.

	--> Accessing the Value
		
		mutableStateOf() returns a State Object.

		Read Value   : count.value

		Update Value : count.value++
		
--> remember()
	remember() stores an Object and returns the same Object during every Recomposition.	
		
	--> Problem without remember()

		Without remember()
		
			@Composable
			fun Counter() {
				val count = mutableStateOf(0)
			}
			
		A Composable can be executed again during recomposition.

		If we simply create the State object inside the Composable,we don't want a completely new State object to replace the
		previous one every time the function is recomposed.
		This is where `remember()` is used.

	Solution : remember()
		`remember()` stores a value in the Composition and returns the remembered value across recompositions.
		
		@Composable
		fun Counter() {

			val count = remember {
				mutableStateOf(0)
			}
		}


	--> What does remember() remember?

		`remember()` remembers the result produced by its lambda.
			Example :
				remember {
					mutableStateOf(0)
				}
			The lambda returns a State Object.
			So remember() remembers the State Object, not the integer value.

--> remember() + mutableStateOf() 

	--> Why are they used together?
			mutableStateOf()	->	  Creates observable State
			remember()			->	Keeps that State associated with the Composition across recompositions
		
		Together:
			
			remember {
				mutableStateOf(0)
			}

			gives us State that survives recomposition.
		
	--> Example
		@Composable
		fun Counter() {

			val count = remember {
				mutableStateOf(0)
			}

			Button(
				onClick = {
					count.value++
				}
			) {
				Text("Count = ${count.value}")
			}
		}

	Flow:
		Initial State	->	 count = 0		->	UI shows "Count = 0"	->	User clicks Button	->	count changes to 1	->	 Compose observes State change	->	Recomposition	->	 UI shows "Count = 1"
		
-->  by (Property Delegation in Compose) 

	--> Without by :
			val count = remember {
				mutableStateOf(0)
			}
			Read	→	count.value
			Update	→	count.value++
			
	--> With by :
			var count by remember {
				mutableStateOf(0)
			}
		Read	→	count
		Update	→	count++

	--> Purpose
		by automatically accesses .value.
		It makes the code shorter and easier to read.
		
	Note : 
		Use val when working without "by"
		Use var when using by because the value itself changes.
		
		Example :
			var count by remember {
				mutableStateOf(0)
			}

				The delegated property itself is being read and assigned:

					count++
					count = 10

				Therefore we use `var`.

			Without `by`, the state-holder reference can remain `val`:

				val count = remember {
					mutableStateOf(0)
				}

				count.value++

--> rememberWindowState()
	`rememberWindowState()` is a Desktop Compose helper used to create and remember a `WindowState`.
	
	Example:
		Window(
			state = rememberWindowState()
		) {
			App()
		}
	
	--> What is WindowState?
		`WindowState` represents the state/configuration of a desktop window, such as:
			• Width	|	• Height	|	• Position	|	• Placement(Normal / Maximized / Fullscreen)
			

	--> Important
		WindowState ≠ Native Window
		WindowState stores information about the window.
		It does NOT itself create the native desktop window.
				
	--> Why rememberWindowState()?
		`rememberWindowState()` remembers the WindowState across recompositions instead of creating a replacement state object each time.
		
		Example:
			Window(
				state = rememberWindowState(
					placement = WindowPlacement.Fullscreen
				)
			) {
				App()
			}
		
		Meaning :
			Create one WindowState object
					↓
			Initial Placement = Fullscreen
					↓
			Remember the same WindowState object across Recompositions.

			
	--> Why not use remember { WindowState() }?

		Because creating and remembering WindowState is a very common requirement.
		Compose provides a helper function :	rememberWindowState()
			
********************************* E. WINDOW CONFIGURATION *********************************
--> Window()

    `Window()` is a Compose Desktop function used to create and configure a desktop application window.
	Example :
		Window(
			onCloseRequest = ::exitApplication,
			title = "ATEK GATE",
			state = rememberWindowState()
		) {

		}
	--> What does Window() do?

		Window() connects our Compose UI with the desktop windowing system.

		It allows us to configure things such as :
			• Window title	|	• Window size / state	|	• Close behavior	|	• Resizability	|	• Window icon	|	• Fullscreen / Maximized placement	|	• Other window properties

	Important :
        Window() does not mean that Compose replaces the OS windowing system.
        The actual native window is still managed by the underlying desktop platform.
	
	--> Window() Content
		`Window()` also accepts a content lambda.

		Example :
			Window(
				onCloseRequest = ::exitApplication
			) {
				App()
			}	
	
--> Window() Parameters
	Common Window() parameters :
		
	1. onCloseRequest
		Example : 
			Window(onCloseRequest = ::exitApplication) {
			}
		Purpose :
			Called when the user clicks the Close (X) button.
		Example :
			onCloseRequest = ::exitApplication
		
		::exitApplication means passes a reference (address) to the function.If we pass like onCloseRequest = exitApplication() then function retur immediately   instead of passing it as the callback.
		
		We can alse be :
			onCloseRequest = {
				saveData()
				exitApplication()
			}
			
	2. title	:	Sets the title displayed by the desktop window.
		Example : 
			Window(
				title = "ATEK GATE"
			)
		Purpose : Sets the Window Title.
		Example : title = "ATEK GATE"

	3. state
		Example :
			Window(
				state = rememberWindowState(
					placement = WindowPlacement.Fullscreen
				)
			)
		Purpose : Passes the WindowState Object to Window().
		Example : state = rememberWindowState()

	4. icon
		Example :
			Window(
				icon = painterResource(...)
			)
		Sets the application's icon.
		
	5. resizable	:	  Controls whether the user can resize the window.
		Example :
			Window(
				resizable = false
			){
				App()
			}
		`false` means the user cannot resize the window.
		Default is generally `true`.
	
	6. undecorated	:	Controls whether the normal operating-system window decorations are displayed.
		Example :
			Window(
				undecorated = true
			)
		 This can remove normal decorations such as the title bar and system borders.
	
	7. transparent	:	  Allows the window to use transparency.
		Example :
			Window(
				transparent = true
			){
				App()
			}
		Allows transparent windows.
			
	8. alwaysOnTop	:	 Controls whether the window stays above other windows.
		Example
			Window(
				alwaysOnTop = true
			){
				App()
			}

--> WindowState
	`WindowState` represents the state/configuration of a Compose Desktop window.

	It can contain information such as :
		• Width		|	• Height	|	• Position	|	• Placement

	Placement can represent states such as :
		• Normal	|	• Maximized		|	• Fullscreen

	--> rememberWindowState()
		`rememberWindowState()` creates and remembers a WindowState across recompositions.

		Example :
			Window(
				state = rememberWindowState(
					placement = WindowPlacement.Fullscreen
				)
			) {
				App()
			}
		--> Why rememberWindowState()?
			WindowState is state associated with the Compose window.
			During recomposition, we do not want to replace the state object unnecessarily.

			`rememberWindowState()` keeps the WindowState associated with the current Composition.

			Important :
				`rememberWindowState()` remembers the WindowState.
				It does NOT create the native window by itself.

********************************* F. APPLICATION CONFIGURATION *********************************
--> application()
	`application()` is a Compose Desktop application function that provides the application environment and desktop application lifecycle.	
	
	Example :
		fun main() = application {
			Window(
				onCloseRequest = ::exitApplication
			) {
				App()
			}
		}
		
	--> Relationship with main()
		`main()` is still the Kotlin/JVM entry point.
		`application()` is called from `main()` to start the Compose Desktop application environment.

	--> application() is a Higher-Order Function
		`application()` accepts a lambda.

		Example :
			application {
				Window(...) {
					App()
				}
			}

		The code inside `{ }` is the lambda passed to application().

		Conceptually :
			application(
				content = {
					Window(...) {
						App()
					}
				}
			)
--> application() Parameters
	The important parameters we need to know here are :
		1. content	|	2. exitProcessOnExit
	
	1. content (Lambda)
		`content` is the lambda containing the code that should run inside the Compose Desktop application environment.
		Example:
			application {
				Window(
					onCloseRequest = ::exitApplication
				) {
					App()
				}
			}
		Conceptually :
			application()	->	Compose application environment	->	content lambda	->	Window()	->	App()
	
	2. exitProcessOnExit
		Purpose : Controls whether the JVM Process exits when the Compose application closes.
		Example :
			application(
				exitProcessOnExit = true
			) {
				Window(
					onCloseRequest = ::exitApplication
				) {
					App()
				}
			}
		Default :   true
			 Application exits
				  ↓
			JVM process exits
			
		`false` :
			Application exits
				  ↓
			JVM process is not automatically terminated by this setting

------------------------------------------------------------------------------------------------------------


####################################### Module 3: PROJECT FOUNDATION : Project setup and then production development. ##########################################################

********************************* A. DEVELOPMENT ENVIRONMENT *********************************

--> Project Flow :
	--> Build / Compile Time

        Developer
            ↓
        IntelliJ IDEA
            ↓
        Gradle
            ↓
        Gradle Plugins
            ├── Kotlin Plugin
            ├── Compose Multiplatform Plugin
            └── Android Plugin (if present)
            ↓
        Dependencies
            ↓
        Compose Compiler Plugin
            ↓
        Kotlin Compiler
            ↓
        JVM Bytecode (.class) / Application Artifacts


    --> Run Time

        JVM
            ↓
        Compose Runtime
            ↓
        Operating System
            ↓
        Application Running

--> IntelliJ IDEA
	IntelliJ IDEA is an Integrated Development Environment (IDE) developed by JetBrains.
	An IDE is a software application that gives you the tools needed to develop software in one place.
	It provides everything required to develop Kotlin and  Compose Multiplatform applications.
	
	Responsibilities : Write code | Auto-completion | Debugging | Run Gradle | Manage projects | Run Application
	
	--> Does IntelliJ compile our code?

		NO. IntelliJ IDEA is the development environment. Think of IntelliJ as the place from which we work
		It starts and coordinates the build process.
		
		The actual build and compilation are performed by Gradle and the configured compiler/toolchain.

--> JDK / JBR
	
	JDK[Java Development Kit] :
		The JDK is a collection of tools and components needed to develop and run JVM-based applications.
		Since our Compose Desktop application runs on the JVM, Java's development/runtime environment is involved even though our source code is Kotlin.
		
		Think:
			Kotlin Project	->	Runs on JVM		->	Needs a compatible Java environment		->		JDK / JBR
	
		--> Why does Kotlin need Java?
			Desktop Compose application is compiled for the JVM.
			Kotlin Code	->	Kotlin Compiler	->	JVM Bytecode	->	JVM		->	Application runs
	
		--> JDK Contains : JVM | JRE | Java Compiler (javac) | Java Libraries | Debugging Tools | Development Tools
	
	JBR[JetBrains Runtime] :
		JetBrains develops IntelliJ IDEA and other IDEs.
		JetBrains also provides its own Java runtime distribution based on OpenJDK, called JetBrains Runtime (JBR).
		
		JetBrains applications such as IntelliJ IDEA are themselves JVM applications.
			IntelliJ IDEA	->	JVM		->		Java Runtime

--> JDK vs JRE vs JVM 	
		
	--> JVM (Java Virtual Machine)

		Purpose : JVM is the actual engine that executes JVM bytecode.
			Kotlin Source Code (.kt)	->	Kotlin Compiler		->		JVM Bytecode (.class)	->	JVM		->	Program Executes
		It does NOT compile code.It only runs compiled Bytecode.	
		
		--> how does JVM execute bytecode?
			
			JIT = Just-In-Time Compiler
			If the JVM notices that calculate() is "hot" — executed frequently — the JIT compiler can compile it into optimized native code.
			The application starts running, and while it is running, the JVM may use JIT to compile frequently executed code into native code.
			
	--> JRE (Java Runtime Environment)
	
		Purpose : Provide everything required to RUN Java Applications.
		JRE Contains : 	• JVM | • Java Libraries
	
	--> JDK (Java Development Kit)
		
		Purpose : Develop Java/Kotlin Applications.
		JDK Contains :	• JRE |	• JVM | • Compiler | • Development Tools

--> Java Version Compatibility
		Different tools in a project must be compatible.
		Java Version Compatibility	->	Can all project tools work together?	->	JDK + Gradle + Kotlin + Compose + Android tools		->	Compatible versions		->		Build succeeds

--> Gradle 	
	Gradle is an Automation and Build Tool.It automates the process of building the project.
	
	--> Responsibilities
		Gradle	-> Download Dependencies	->	Compile Source Code	->	Run Compose Compiler	->	Create Build Files	->	Run Application
	
	--> What does Gradle actually do?
		Gradle reads your project configuration and performs the required build tasks.
		For example:
			Developer
				↓
			Gradle
				├── Resolve plugins
				├── Download dependencies
				├── Configure project
				├── Compile Kotlin
				├── Run Compose-related compilation
				├── Process resources
				├── Package application
				└── Run application
				

	--> Gradle is not only for Kotlin	
		It can be used for : Java	|	Kotlin	|	Android		|	Kotlin Multiplatform	|	Compose Multiplatform
	
	--> Why do we need Gradle?
		Without Gradle, we would have to manually :

			• Download Libraries	|	• Configure Compiler	|	• Compile Source Code	|	• Package the Application	|	• Run the Application

		Gradle automates all these tasks.
	
	--> What are Gradle Tasks?
		A task is basically a specific operation Gradle knows how to perform.

		Examples :
			compileKotlin	|	test	|	build	|	run	|	package
		
		A larger task can depend on other tasks.
			For example : build	->	compile	->	resolve dependencies
			So Gradle knows what needs to happen and in what order.
	
	--> Gradle Plugins
		A Gradle plugin adds functionality/configuration to Gradle.
			Conceptually : Gradle	->	Plugins add capabilities	->	Kotlin support	|	Compose support	|	Android support
		
		For example:
			Kotlin Plugin	|	Compose Multiplatform Plugin	|	Android Gradle Plugin
	
	--> Gradle Dependencies
		application doesn't contain every library itself.
		For example:
			dependencies {
				implementation(...)
			}
		tells Gradle:
			"My project needs this library." Gradle then resolves/downloads the required dependency from configured repositories
			
	--> Important Gradle files	:	
		Common Gradle Files :	settings.gradle.kts	|	build.gradle.kts	|	gradle.properties
		
		--> settings.gradle.kts		:	Which projects/modules exist?
			defines project-level settings
				• Project Name | • Included Modules | Plugin management / repositories
		
		
		--> build.gradle.kts	:	How should this project/module be built?
			Defines how the project/module is built.
				• Plugins | • Dependencies | 	• Project Configuration | • Build Configuration
			
			--> .gradle.kts — why Kotlin?
				Because this is a Kotlin DSL(Domain-Specific Language)  file.
				.gradle	   		 → Gradle script
				.gradle.kts		 → Gradle script written using Kotlin DSL
				
		--> gradle.properties	:	Configuration values for the Gradle build.
			Contains Gradle/project properties and configuration.
				Gradle JVM arguments | Build-related properties |  Project properties
					
--> Kotlin Compiler 

	The Kotlin Compiler does NOT execute the application.It only converts the source code into Bytecode.
	
	Input  : Kotlin Source Files (.kt)
	Output : JVM Bytecode (.class)
	
	--> Flow
		Kotlin Source Code
				↓
		Kotlin Compiler
				↓
		JVM Bytecode (.class)
	
--> Compose Compiler Plugin 

	The Compose Compiler Plugin extends the Kotlin Compiler.It teaches the Kotlin Compiler how to understand Compose-specific code.
	Works at : Compile Time

	Purpose :
		Process @Composable code
                ↓
        Transform Compose-specific code
                ↓
        Generate JVM-compatible compiled code
                ↓
        Kotlin compilation process
	
--> Compose Multiplatform Plugin 
	
	It is a Gradle Plugin.It configures the project for Compose Multiplatform development.

	--> Responsibilities
		• Configure Compose Project | • Add Compose Dependencies | • Configure Desktop Target | • Configure Android Target (if present) | • Configure Build Tasks
			
--> Compose Runtime 	
	
	It starts after the JVM starts the application.
	Compose Runtime is the library responsible for running Compose applications.
	
	Compose Compiler Plugin	Works at : Compile Time
	Compose Runtime	Works at 		 : Run Time
	
	Responsibilities : • Execute Composable Functions	|	• Manage Composition	|		• Manage State	|	• Detect State Changes	|	• Perform Recomposition
	
	--> Without Compose Runtime
	Even if the project compiles successfully, the application cannot display Compose UI.
		
--> Dependency 

	A Dependency is an external Library or Framework that our project requires to work.
	Instead of writing everything ourselves, we reuse existing libraries.
	Examples :	Compose UI | Coroutines | Serialization | SQLite | Ktor

	--> Where are Dependencies added?
		In Gradle.
		Example :
			dependencies {
				implementation(...)
			}
		implementation tells Gradle : "This project depends on this library."
	

********************************* B. PROJECT CREATION *********************************	
There are two ways to create a Compose Multiplatform Project.
	Method 1	:	Official Project Generator(Project generated automatically)		|		Method 2	:	Manual Project Creation(Developer creates every file manually)

-->  Method 1 - Official Project Generator 
	The Official Project Generator creates a ready-to-use Compose Multiplatform project.
	It automatically generates :	Project Structure	|	Gradle Configuration	|	Source Structure	|	Sample Application	|	Build Configuration

--> Why did the Compose Project Wizard change?
	Earlier, IntelliJ IDEA contained a built-in Compose Project Wizard.
	Later, JetBrains moved project generation to the official website.
	
	Reason : 
		Project Templates change frequently.
		Keeping templates on the website allows JetBrains to update them without waiting for a new IntelliJ release.

--> Kotlin Multiplatform Project Wizard

    Kotlin Multiplatform provides an official project generator/wizard for creating Kotlin Multiplatform projects.

    Website : https://kmp.jetbrains.com/

    Project Name : Human-readable name of the project/folder.
        Example : ComposeLearning

    Project ID : Unique identifier of the project.
        Example : com.atek.composelearning
		
		
--> Generated Project Structure
	The Official Project Generator creates a Multi-Module project.
	
    Example :

        ProjectBasic01
            │
            ├── .gradle
            ├── .idea
            ├── androidApp
            ├── desktopApp
            ├── gradle
            ├── shared
            │
            ├── .gitignore
            ├── build.gradle.kts
            ├── gradle.properties
            ├── gradlew
            ├── gradlew.bat
            ├── local.properties
            ├── README.md
            └── settings.gradle.kts
	
	--> Why Multi-Module?
		Compose Multiplatform separates the project into multiple modules.
		
		androidApp     → Android-specific application code/configuration
        desktopApp     → Desktop-specific application code/configuration
        shared         → Common code shared between platforms
	
--> Opening the Project

    After opening the generated project,

        IntelliJ IDEA loads the project.
                ↓
        IntelliJ detects the Gradle project by seeing files : build.gradle.kts | settings.gradle.kts | etc
                ↓
        Gradle Sync starts.

--> What is Gradle Sync?
	
	When IntelliJ opens a Gradle project, it needs to understand the project's Gradle configuration.

	Gradle Sync is the process where IntelliJ and Gradle work together to load the project's configuration and make the required dependencies, plugins, modules, source sets, and build information available to the IDE.	
	
	Basic flow:
		Project Opened
			  ↓
		IntelliJ detects Gradle project
			  ↓
		Gradle Sync
			  ↓
		Read Gradle configuration
			  ↓
		Resolve plugins & dependencies
			  ↓
		Configure project in IntelliJ
			  ↓
		Project Ready
		
	--> What does Gradle Sync read?
		
		settings.gradle.kts		: tells Gradle about the project and its modules.
		build.gradle.kts		: tells Gradle how those projects/modules are configured and built.
		gradle.properties		: provides Gradle/project properties.
	
	--> What happens during Sync?
		Gradle Sync performs the following tasks :

			Read settings.gradle.kts
				↓
			Identify project/modules
				↓
			Read build.gradle.kts
				↓
			Load required plugins
				↓
			Resolve dependencies
				↓
			Download missing components
				↓
			Configure project
				↓
			IntelliJ understands the project
			
	--> Why is Gradle Sync required?
			Because IntelliJ cannot know everything about your project just by looking at the folders.
			IntelliJ needs Gradle to tell it things such as:
				Which are Gradle modules?	|	Which plugins are applied?	|	Which dependencies are required?	|	Which source sets exist?	|	Which targets are configured?	|	Which build tasks exist?
	

	--> Run Generated Project

		Click Run	->	IntelliJ IDEA	->	Gradle	->	Resolve required configuration/dependencies	->	Compile Kotlin + Compose code	->	Create JVM bytecode / application artifacts	->	Start JVM application	->	Compose Runtime	->	Compose UI	->	Application Starts

--> Real-World Issue[to run android application] — AGP Compatibility

    AGP = Android Gradle Plugin.

    Initial Project :
        AGP = 9.1.1
        compileSdk = 37
        targetSdk = 37
        Gradle = 9.5.1

    --> Problem
        IntelliJ supports AGP up to 9.0.0, but the generated project uses AGP 9.1.1.

        Sync → AGP Compatibility Error

    --> Fix for IntelliJ

        In libs.versions.toml:

            agp = "9.0.0"
            android-compileSdk = "36"
            android-targetSdk = "36"

        Gradle = 9.5.1 → Keep unchanged.

        Then:
            Gradle Sync → BUILD SUCCESSFUL


 --> Run Desktop Application

     Right Panel → Gradle
         ↓
     desktopApp
         ↓
     Tasks
         ↓
     compose desktop
         ↓
		run
         ↓
     Desktop Application


--> Run Android Application

    Top-right Run Configuration
        ↓
    androidApp
        ↓
    Select Android Device / Emulator
        ↓
    Run ▶
        ↓
    Android Application

********************************* C. GRADLE FUNDAMENTALS *********************************	
--> Gradle Import & Build Flow

	When IntelliJ shows	:	Link Gradle Project		and we click it,

	Flow :	IntelliJ	->	gradlew / gradlew.bat	->	Read settings.gradle.kts	->	Get Plugin Versions		->		Get Plugin Repositories		->		Download Plugins
																																								|
																																								|
																																								|
					Configure Kotlin Plugin		<-	  Download Libraries	<-	   Read Library Repositories 	<-	  Load Plugins 		<-	  Read build.gradle.kts
						|
						|
						|
			Configure Compose Plugin
						|
						|->		Build Project		->		Compile Kotlin		->		 Generate .class		->	Package Application		->	Run
						|	
						|->		Import Project 

--> Gradle

    Gradle is a Build Automation System.
    It coordinates the process of building, testing, packaging and running the project.


--> Gradle Tasks

    A Gradle Task is a specific unit of work that Gradle can execute.

    Examples :
        build   → Build the project
        clean   → Remove build outputs
        test    → Run tests
        run     → Run the application

    --> Our Desktop Project

        Right Panel → Gradle
            ↓
        desktopApp
            ↓
        Tasks
            ↓
        compose desktop
            ↓
        run


--> Task Dependency
    A Gradle Task can depend on another Task.

    Example :

        run
         ↓
        jar
         ↓
        classes

    Meaning :
        run depends on jar.
        jar depends on classes.

    When we execute run, Gradle automatically executes the required tasks in the correct order.

    --> UP-TO-DATE
        Gradle skips a task when its existing output is already valid and does not need to be rebuilt.

    Example :
        classes → UP-TO-DATE
        jar     → UP-TO-DATE
        run     → EXECUTED


--> Plugins
    A Gradle Plugin adds/configures specific build functionality in Gradle.

    Examples :
        Kotlin Multiplatform Plugin
        Compose Multiplatform Plugin
        Android Gradle Plugin (AGP)
        Compose Compiler Plugin

    Example :
        Gradle
            ↓
        Plugin
            ↓
        Adds/configures required functionality

    --> Plugin vs Dependency

        Plugin     → Adds/configures build functionality.
        Dependency → Provides library functionality to application code.


--> Dependencies
    A Dependency is an external Library or Framework that our project requires.
    Instead of writing everything ourselves, we reuse existing libraries.

    Examples :
        Compose UI | Coroutines | Serialization | SQLite | Ktor

    --> Where are Dependencies added?
        In Gradle.

        Example :
            dependencies {
                implementation(...)
            }

        implementation tells Gradle :
            "This project depends on this library."


--> Application Packaging
    Application Packaging prepares the built application and required resources into a distributable format.

    Desktop packaging tasks can be found at:

        Right Panel → Gradle
            ↓
        desktopApp
            ↓
        Tasks
            ↓
        compose desktop

    Examples :
        packageReleaseMsix	            → Windows MSIX package
        packageReleaseDeb	            → Linux DEB package
		packageReleaseDmg	            → macOS DMG package
	
	Flow 					 : 	Main.kt		->		Kotlin Compiler		->		.class files	->		Gradle Packages Everything		->		Application.jar

	Application.jar Contains : .class files	|	Compose Runtime	|	Kotlin Runtime	|	Other Libraries


--> Runtime Execution
    Runtime means the actual execution of the built application.

    Desktop Flow :

        Gradle	->	Required Tasks	->	Build / Compile	->	Application Artifacts/.class	->	JVM	->	main()	->	Compose Runtime	->	UI	->		->	Application Running


--> Important Points

    Gradle       → Build Automation System
    Task         → Unit of work performed by Gradle Task Dependency	|	One task requires another task
    Plugin       → Adds/configures build functionality
    Dependency   → External library required by the application
    Packaging    → Creates a distributable application
    Runtime      → Actual execution of the application

********************************* D. PROJECT CONFIGURATION *********************************	

--> Android SDK(Software Development Kit)

    Android SDK = collection of tools, libraries and platform files required for Android development.

    Important Parts:
        Android Platform / API	|	Build Tools	|	Platform Tools	|	Other Android Development Tools

    Example:
        android-compileSdk = "36"	        → Compile Android application against Android API 36.

    Android SDK → Android platform/tools.
    AGP 		→ Android build functionality.


--> SDK Manager
    SDK Manager is the tool used to install, update and manage Android SDK components.

    Example:
        Android SDK Platforms	|	Build Tools	|	Platform Tools


--> Android Gradle Plugin (AGP)
    AGP is a Gradle plugin that adds Android-specific build functionality to Gradle.

    Gradle	->	AGP	->	Android Build	->	Android SDK

    Example :      agp = "9.0.0"


--> Kotlin Plugin
    Kotlin Plugin integrates Kotlin with Gradle and configures Kotlin compilation.

    Gradle	->	Kotlin Plugin	->	Kotlin Compiler	->	JVM Bytecode	->	Kotlin Plugin ≠ Kotlin Compiler.


--> Device Manager
    Device Manager is used to create and manage Android Virtual Devices (AVD).


--> Android Emulator
    Android Emulator is software that behaves like a virtual Android device.

    Computer	->	Android Emulator	->	Virtual Android Device	->	Android Application


--> Physical Android Device
    A real Android phone/tablet can be used to run and test the application.

    Usually requires : Developer Options	|	USB Debugging


--> ADB (Android Debug Bridge)
    ADB is a tool that allows the computer to communicate with an Android device or emulator.

    Computer	->	ADB		->	Android Device / Emulator

    Example:
        adb devices	        → Shows connected Android devices/emulators.

--> Environment Variables
    Environment variables are system-level configuration values that tools can read.

    Example:
        PATH	|	ANDROID_HOME

    They can help tools locate required executables or development resources.

    Project-specific Android SDK location can also be configured through local.properties.


--> Complete Configuration Flow

    IntelliJ IDEA
        ↓
    Gradle
        ↓
    Kotlin Plugin + Compose Plugin + AGP
        ↓
    Kotlin / Android Build
        ↓
    SDK / Required Tools
        ↓
    Emulator / Physical Device
        ↓
    Android Application

********************************* E. RUNNING THE APPLICATION *********************************	
--> Desktop Target
    Desktop application can be run through:

        Gradle
            ↓
        desktopApp
            ↓
        Tasks
            ↓
        compose desktop
            ↓
        run

       This executes the Desktop application on Windows.

    We also have a Desktop Run Configuration in IntelliJ, which can be selected from the top-right Run Configuration selector and started using Run ▶.

--> Android Emulator

    Android Emulator = virtual Android device running on the development computer.

    --> Create Emulator

			IntelliJ
				↓
			Device Manager
				↓
			Create Virtual Device
				↓
			Select Device Profile
				↓
			Select System Image
				↓
			Download System Image if required
				↓
			Create Virtual Device
				↓
			Start Emulator

    --> Device Profile
        Defines the virtual device's hardware/screen characteristics.

        Example:
            Resizable (Experimental)

        Useful when testing responsive UI across different screen sizes.

    --> System Image
        System Image = Android OS image that the emulator runs.

        Example:
            API 36
            Android 16
            Google APIs
            Intel x86_64

        API Level → Android platform/API version.
        Google APIs → Google APIs included in the image.
        x86_64 → CPU architecture of the emulator image.

    --> Run Application
			Android Run Configuration
					↓
			Select Emulator
					↓
			Run ▶
					↓
			Android Application


--> Physical Android Device
    A real Android phone/tablet can be used instead of an emulator.

    --> Required Setup
			Physical Android Device
					↓
			Developer Options
					↓
			USB Debugging → ON
					↓
			Connect device to computer
					↓
			Authorize computer on device
					↓
			Device becomes available


	--> ADB (Android Debug Bridge)
		ADB is the communication bridge between the computer and Android devices/emulators.

			Computer
				↓
			   ADB
				↓
			Android Device / Emulator

--> Device Detection

    Device Detection = Development tools discovering available Android devices.

    ADB is used to communicate with Android devices.
        adb devices
            ↓
        Connected Devices

    Example:

        List of devices attached
        ABC123456    device

    "device" → Device is connected and authorized.


--> Run Configurations
    Run Configuration tells the IDE what application/target should be executed and how it should be run.

    Example:
        DesktopApp
            ↓
        Windows Desktop

        Android
            ↓
        Emulator / Physical Device

    Run Configuration ≠ Gradle Task
        Gradle Task	            → Build-system operation.
        Run Configuration	    → IDE configuration used to launch a target.

********************************* F. PROJECT STRUCTURE *********************************	

--> Root Project

    ProjectBasic01 = Root Project.

    Root Project = top-level Gradle project/container for the complete build. It is NOT itself the executable application.

    It contains modules such as:
        ProjectBasic01
        ├── androidApp
        ├── desktopApp
        └── shared


--> Gradle Project vs Module

    Gradle Project	        → a project Gradle knows how to configure/build.
    Module			        → a distinct part of the project with its own responsibility, source code and build configuration.

    Our root project contains multiple modules.


--> settings.gradle.kts and Module Inclusion

    `settings.gradle.kts` defines the structure of the Gradle build.
    It tells Gradle which modules belong to the root project.

    Example:
        rootProject.name = "ProjectBasic01"

        include(":androidApp")
        include(":desktopApp")
        include(":shared")

    Therefore Gradle knows:
        ProjectBasic01
        ├── androidApp
        ├── desktopApp
        └── shared


    `:` represents the root project.

    Example:
        :desktopApp:run

    means:
        run the `run` task inside the `desktopApp` module.


--> Important
    `rootProject.name = "ProjectBasic01"`
	does NOT determine the Kotlin package name.
		Example Kotlin package : package com.atek.projectbasic

--> Why Multi-Module?
    Separates responsibilities.

    Benefits :   Better organization	|	  Reusable code	|	 Easier maintenance	|	 Clear dependencies		|	Platform separation


--> Current Relationship

        ProjectBasic01
        │
        ├── androidApp  → Android
        ├── desktopApp  → Desktop
        └── shared      → Common/multiplatform code

    Platform modules can depend on shared:

        androidApp ─────→ shared
        desktopApp ─────→ shared


    shared should generally NOT depend on platform application modules.


--> Current Compose Multiplatform Project Structures

    Different projects/templates can use names such as:
        androidApp	|	 desktopApp		|	shared		|		composeApp		|	  iosApp	|	 etc.

    `composeApp` is simply a module name used by some project templates. It is NOT a separate technology.

--> Important
    Module/folder names do not define the architecture by themselves.
    Responsibilities and dependencies define the architecture.
    If a production project contains:
        composeApp

    inspect what it actually contains instead of assuming what the module does.
	
	
	--------------------------------- Modules ----------------------------------------------------
	
--> Module
    A major independently configured part of the project.
    It can have :  Source code	|	 Dependencies	|	Gradle configuration	|	Its own responsibility

--> shared
    `shared` is the common/multiplatform module.
    It contains code that can be shared between supported platforms.

    Example:
        commonMain
            → common application/business code


--> desktopApp
    `desktopApp` is the Desktop application module.
    It contains Desktop-specific application configuration and code.

    It can depend on :  shared
	
--> androidApp
    `androidApp` is the actual Android application module.

    Typical responsibilities include:
        MainActivity	|	AndroidManifest	|	Android application setup	|	Android-specific application configuration

    It can depend on : shared

--> composeApp

    `composeApp` is a module name used by some Compose Multiplatform project templates.
    It is NOT a special Compose technology.
    What it contains depends on the project's architecture/template.

--> Module Relationship

    Current project:
        ProjectBasic01
        │
        ├── androidApp
        │       │
        │       └────→ shared
        │
        ├── desktopApp
        │       │
        │       └────→ shared
        │
        └── shared

    Platform application modules use code from the shared module.

--> Module Dependencies

    Example from `desktopApp/build.gradle.kts`:
        implementation(project(":shared"))


    Example from `androidApp/build.gradle.kts`:
        implementation(project(":shared"))

    This creates:
        desktopApp → shared
        androidApp → shared


    1. `project(":shared")`	        → refers to the `shared` module inside the current Gradle build.

    2. `implementation`
        → makes that dependency available to the module for compilation/runtime.

    Important:
        shared should generally NOT depend on androidApp or desktopApp.

    The dependency direction should normally be:
        androidApp ─────→ shared
        desktopApp ─────→ shared

 --------------------------------- Source Sets ----------------------------------------------------

	
--> Source Set
    A Source Set = group of source files inside a module intended for a particular platform or purpose.


	--> Hierarchy
			Project	->  Modules	->	 Source Sets	->	 Source Files
		
		Example:
			ProjectBasic01	->	 shared		->	 commonMain		->		Kotlin source files

--> Actual shared/src

    shared
    └── src
        ├── androidHostTest
        ├── androidMain
        ├── commonMain
        ├── commonTest
        ├── jvmMain
        └── jvmTest


	--> commonMain
		Common production code.
		Intended to be shared across supported platforms.


	--> commonTest
		Tests for common/shared code.


	--> androidMain
		Android-specific production code inside the shared module.

		Important:
			androidMain       ≠	     Android code shared with Desktop.

		It means:
			Android-specific code inside the multiplatform/shared module.


	--> androidHostTest
		Android host-side tests.


	--> jvmMain
		JVM-specific production code.
		In the current project it is relevant to the Desktop/JVM side.


	--> jvmTest
		Tests for JVM-specific code.


--> Platform-specific Source Sets
    Used when code must be different for a particular platform.

    Example:
        commonMain	            → common implementation
        androidMain	            → Android-specific implementation
        jvmMain	            → JVM/Desktop-specific implementation


--> Source Set Dependency

    Common code can be used by platform-specific source sets.

    Conceptually:

        commonMain
           ↓
        ┌───────────────┐
        ↓               ↓
    androidMain      jvmMain


	--> expect / actual

		commonMain can declare expected platform-specific functionality.
		Platform-specific source sets provide the actual implementation.

		Conceptually:

			commonMain
				expect
				  ↓
			┌───────────────┐
			↓               ↓
		androidMain       jvmMain
			actual           actual


--> androidApp vs shared/androidMain

    androidApp
        → actual Android application module.

        Typical responsibilities : MainActivity		|	AndroidManifest		|	 Android application setup		|	Android-specific app configuration

    shared/androidMain
        → Android-specific implementation inside the shared multiplatform module.


    Both can contain Android code, but they have different architectural ownership.


	--> Example
		commonMain	        → LoginRepository
		androidMain	        → Android-specific token storage
		jvmMain	        → Desktop-specific token storage

	--------------------------------- FOLDER OWNERSHIP ----------------------------------------------------

--> build
    `build` contains generated build output.
    It is created by Gradle while building the project.

    It can contain things such as:
        Compiled files	|	Generated files		|	Build artifacts		|	 Intermediate build output

    Important:
        build	            → generated output
        src		            → source code written by developers

    `build` is NOT application source code. It can generally be deleted and recreated by Gradle.


--> .gradle
    `.gradle` contains Gradle's project-specific working/cache data.
    It is used by Gradle while working with the project.
    It is NOT application source code.

    Think:
        .gradle	            → Gradle's working/cache area	

--> .idea
    `.idea` contains IntelliJ IDEA project configuration.

    It can contain IDE-specific settings such as:
        Project configuration	|	IDE settings	|	Run configurations	|	Other IntelliJ metadata


    Important:
        .idea	            → IntelliJ's project information
        .gradle	            → Gradle's project information


--> .kotlin
    `.kotlin` contains Kotlin-related generated/tooling data.
    It is related to Kotlin build/tooling support.
    It is NOT where we normally write application source code.


--> gradle
    `gradle` contains Gradle Wrapper supporting files.

    The Gradle Wrapper allows the project to use the Gradle version expected by the project instead of depending on a globally installed Gradle version.

    Important:
        gradle/	            → Wrapper supporting infrastructure


--> src
    `src` contains the actual source code.
    In a multi-module project, each module can have its own `src`.

    Example:
        desktopApp
        └── src

        androidApp
        └── src

        shared
        └── src

    Inside `shared/src` we have source sets such as:

        commonMain	|	 commonTest	|	androidMain	|	androidHostTest	|	jvmMain	|	jvmTest


--> build.gradle.kts

    Root-level Gradle build configuration.

    Our actual root file was:

        plugins {
            // this is necessary to avoid the plugins to be loaded multiple times in each subproject's classloader

            alias(libs.plugins.androidApplication) apply false
            alias(libs.plugins.androidMultiplatformLibrary) apply false
            alias(libs.plugins.composeMultiplatform) apply false
            alias(libs.plugins.composeCompiler) apply false
            alias(libs.plugins.kotlinJvm) apply false
            alias(libs.plugins.kotlinMultiplatform) apply false
        }


	--> plugins {}

		`plugins {}` declares Gradle plugins used/available for the project.
		A plugin adds specific build functionality/configuration to Gradle.

		Examples:
			Android Application	|	Kotlin/JVM	|	Kotlin Multiplatform	|	Compose Multiplatform	|	Compose Compiler


		--> alias(...)

			Example : alias(libs.plugins.kotlinJvm)

			gets the plugin definition from the project's Version Catalog :   gradle/libs.versions.toml

			So:
				libs.plugins.kotlinJvm
					→ plugin definition/version from Version Catalog


		--> apply false

			Example : alias(libs.plugins.kotlinJvm) apply false

			Means:
				The plugin is declared/available here, but it is NOT applied to the root project itself.

			Individual modules can then apply the plugins they need.

			Example:
				desktopApp
					→ Kotlin/JVM
					→ Compose Multiplatform
					→ Compose Compiler

				androidApp
					→ Android Application
					→ Compose Compiler


	--> Why root build.gradle.kts has these plugins?
		The root project declares the plugins centrally.
		Modules can then use the required plugins without having the root project itself use those plugins as an application module.


	--> Important

		Root `build.gradle.kts` does NOT mean : "Build the Desktop application here."

		It is root-level Gradle configuration.
		The actual module build files contain module-specific configuration.


--> settings.gradle.kts
    `settings.gradle.kts` defines/sets up the Gradle build structure.

    It tells Gradle:
        What is the root project?	|	Which modules belong to the build?	|	Where can plugins be resolved?	|	Where can dependencies be resolved?

    Our actual file was :

        rootProject.name = "ProjectBasic01"

        pluginManagement {
            repositories {
                google {
                    mavenContent {
                        includeGroupAndSubgroups("androidx")
                        includeGroupAndSubgroups("com.android")
                        includeGroupAndSubgroups("com.google")
                    }
                }
                mavenCentral()
                gradlePluginPortal()
            }
        }

        dependencyResolutionManagement {
            repositories {
                google {
                    mavenContent {
                        includeGroupAndSubgroups("androidx")
                        includeGroupAndSubgroups("com.android")
                        includeGroupAndSubgroups("com.google")
                    }
                }
                mavenCentral()
            }
        }

        plugins {
            id("org.gradle.toolchains.foojay-resolver-convention")
                version "1.0.0"
        }

        include(":androidApp")
        include(":desktopApp")
        include(":shared")


	--> rootProject.name
		Example :  rootProject.name = "ProjectBasic01"

		Gives the root Gradle project its name.
		It does NOT determine the Kotlin package name.


	--> pluginManagement
		`pluginManagement` controls plugin resolution configuration.

		Example:
			pluginManagement {
				repositories {
					...
				}
			}

		The repositories tell Gradle where it can look for Gradle plugins.

		Our project uses:
			google()	|	 mavenCentral()	|	gradlePluginPortal()


		--> includeGroupAndSubgroups

			Our Google repository configuration contains:
				includeGroupAndSubgroups("androidx")	|	includeGroupAndSubgroups("com.android")	|	includeGroupAndSubgroups("com.google")

			This limits which groups are considered from the Google repository.

			We do NOT need to memorize this configuration now.

			Important concept:

				pluginManagement
					→ plugin resolution configuration


	--> dependencyResolutionManagement
		Defines how Gradle resolves external libraries/dependencies.

		Our project configures:
			google()	|	mavenCentral()



	--> settings-level plugins

		Our settings file contains:
			plugins {
				id("org.gradle.toolchains.foojay-resolver-convention")
					version "1.0.0"
			}


		This is a Gradle plugin applied at the settings/build level.
		Detailed Foojay behavior is not required to memorize now.


	--> include(...)

		Example:
			include(":androidApp")
			include(":desktopApp")
			include(":shared")

		Tells Gradle that these modules belong to the root build.


		--> `:` meaning
			`:` represents the root project.

			Therefore:
				:shared	            → shared module in the root project
				:desktopApp	        → desktopApp module in the root project
				:desktopApp:run		→ run task inside desktopApp



--> gradle.properties

    `gradle.properties` contains Gradle/project properties.

    It can contain project-wide configuration related to things such as:
        Gradle behavior	|	Build settings	|	Performance/configuration properties

    Think:
	     gradle.properties	       → project/Gradle properties


--> local.properties
    `local.properties` contains machine-specific local configuration.

    Common example:
        Android SDK location


    It is specific to the developer's machine and normally should not be treated as shared project configuration.


--> gradlew

    Gradle Wrapper launcher for : Linux	|	macOS

    It allows the project to use its configured Gradle version.


--> gradlew.bat

    Gradle Wrapper launcher for :  Windows

    Example :  .\gradlew.bat build


    The Wrapper uses the project's configured Gradle version instead
    of relying on whatever Gradle version happens to be installed
    globally.


--> Gradle vs Gradle Wrapper

    Gradle        			→ actual build system/manager.

    Gradle Wrapper	        → project-controlled mechanism for using the required Gradle version.

    gradlew 		        → Linux/macOS launcher.

    gradlew.bat			    → Windows launcher.

    gradle/			        → Wrapper supporting files.



--> libs.versions.toml

    `libs.versions.toml` is the project's Version Catalog.

    It provides a central place for defining:
        Library versions	|	Plugin versions	|	Dependency aliases	|	Plugin aliases

    Example:
        libs.plugins.kotlinJvm	|	libs.plugins.composeCompiler	|	libs.kotlinx.coroutinesSwing


    Instead of repeating dependency/plugin information in every Gradle file, modules can reference the centralized definitions.


--> README.md

    `README.md` contains project documentation.

    It can explain:
        What the project is		|	How to build it		|	How to run it	|	Setup instructions	|	Other project information


--> .gitignore

    `.gitignore` tells Git which files/folders should NOT normally be tracked in the repository.

    Common examples include:
        Generated build output	|	IDE-specific files	|	Machine-specific files	|	Other local/generated data


--> Root Structure Mental Model

    ProjectBasic01
    │
    ├── .gradle              → Gradle working/cache data
    ├── .idea                → IntelliJ configuration
    ├── .kotlin              → Kotlin tooling data
    ├── gradle               → Gradle Wrapper infrastructure
    │
    ├── androidApp
    │   └── src              → Android source code
    │
    ├── desktopApp
    │   └── src              → Desktop source code
    │
    ├── shared
    │   └── src              → Multiplatform source sets
    │
    ├── build.gradle.kts     → Root Gradle configuration
    ├── settings.gradle.kts  → Build structure/module inclusion
    ├── gradle.properties    → Gradle/project properties
    ├── local.properties     → Machine-specific configuration
    ├── gradlew              → Wrapper launcher (Linux/macOS)
    ├── gradlew.bat          → Wrapper launcher (Windows)
    ├── README.md            → Project documentation
    └── .gitignore           → Git ignore rules

--------------------------------- BUILD LIFECYCLE ----------------------------------------------------


--> Complete Build + Runtime Flow

    BUILD TIME
		
		↓
		
		Click Run
			↓
		Gradle Starts
			↓
		Read project configuration - `settings.gradle.kts`
			↓
		Configure Project
			↓
		Read build.gradle.kts
			↓
		Resolve Plugins
			↓
		Resolve Dependencies
			↓
		Compile Kotlin
			↓
		Compose Compiler
			↓
		Generate JVM Bytecode
			↓
		Package / Prepare Application
       
	   ↓
	   
    RUNTIME
        
		JVM Starts
			↓
		main()
			↓
		application()
			↓
		Window()
			↓
		Compose Runtime
			↓
		UI Displayed


--> 1. Click Run

    When we click Run in IntelliJ, IntelliJ knows which application or target we want to launch.
    IntelliJ coordinates the process.
    IntelliJ itself does NOT compile the Kotlin source code.

    For our Desktop application, the process eventually uses Gradle and the Desktop run task.


--> 2. Gradle Starts
    Gradle takes control of the build/run process.

    It needs to understand:
        Project structure	|	Modules	|	Plugins	|	 Dependencies	|	Tasks	|	Build configuration


--> 3. Read settings.gradle.kts
	Gradle uses `settings.gradle.kts` to understand the structure  of the Gradle build.

    Example:
		rootProject.name = "ProjectBasic01"

        include(":androidApp")
        include(":desktopApp")
        include(":shared")


--> 4. Configure Project

    Gradle configures the root project and its modules.

    It processes the Gradle configuration to understand:
        Module type	|	Plugins	|	Dependencies	|	Tasks	|	Build settings

    This is called the configuration phase.

    Simple meaning:
        Gradle first understands how the project is configured before performing the required build task.


--> 5. Read build.gradle.kts
    Gradle processes the relevant `build.gradle.kts` files.

    For our Desktop application, the important module is:
        desktopApp/build.gradle.kts

    It contains information such as:
        Plugins		|     Dependencies	|	mainClass	|	Desktop configuration	|	Native distribution configuration


    Example:
        plugins {
            alias(libs.plugins.kotlinJvm)
            alias(libs.plugins.composeMultiplatform)
            alias(libs.plugins.composeCompiler)
        }


--> 6. Resolve Plugins
    Gradle identifies and obtains the plugins required by the project/module.

    Our Desktop module uses plugins such as:
        Kotlin/JVM	|	Compose Multiplatform	|	Compose Compiler


    Plugin resolution means:
        "Find the required Gradle plugins and make them available to configure/build this project."


--> 7. Resolve Dependencies
    Gradle determines which libraries and modules are required.

    Example:
        implementation(project(":shared"))
        implementation(compose.desktop.currentOs)
        implementation(libs.kotlinx.coroutinesSwing)

    If a required external dependency is not already available locally, Gradle obtains it from the configured repositories.


--> 8. Compile Kotlin

    Now the Kotlin source code is compiled.

        Kotlin Source Code
              ↓
        Kotlin Compiler
              ↓
        JVM Bytecode


--> 9. Compose Compiler
    Compose-specific compilation support processes Compose-related code during the Kotlin compilation process.

    Example:
        @Composable
        fun App() {
            ...
        }

    Compose code requires special compiler support so that it can work correctly with Compose Runtime.

    Important:

        Compose Compiler	   ≠	     completely separate compiler replacing Kotlin Compiler.
		It is part of the Compose-aware compilation process.


--> 10. Generate JVM Bytecode

    Kotlin source code is ultimately converted into JVM bytecode
    JVM bytecode is the form that the JVM can execute.


--> 11. Package Application
    Gradle prepares the compiled application and required resources for running or distributing it.

    For Desktop, Compose Desktop can also be configured to create native distribution formats:
        MSI	            → Windows
        DMG	            → macOS
        DEB	            → Linux


    Important:
        Normal development Run	            → does NOT necessarily create final installers for  every platform.
        Distribution/package task	        → can create the configured native packages.


--> 12. JVM Startup

    After the application has been prepared for execution, the JVM starts the Desktop application.

    JVM:
        Loads the required classes	|	Starts the application	|	Executes JVM bytecode

--> 13. main()
    `main()` is the entry point of the Kotlin application.

    Example:
        fun main() {
            ...
        }

    Our Desktop configuration contains:
        mainClass = "com.atek.projectbasic.MainKt"


    A top-level Kotlin `main()` function is compiled into a JVM class.

    Therefore:
        com.atek.projectbasic.MainKt    represents the generated JVM class containing the top-level `main()` function.


--> 14. application()

    Our Desktop application can have:
        fun main() = application {
            ...
        }


    `application()` establishes the Compose Desktop application environment and lifecycle.

    Simple meaning:
        application()	            → starts/establishes the Compose Desktop application environment.

    It provides the application-level environment in which Desktop windows can exist.


--> 15. Window()

    Inside the application:
        Window(
            onCloseRequest = ::exitApplication
        ) {
            App()
        }


    `Window()` creates/configures the Desktop application window and provides the Compose content displayed inside that window.


--> 16. Compose Runtime Startup
    Now Compose Runtime manages the Compose UI.

    Compose Runtime handles mechanisms such as:
        Composition	|	 State	|	Recomposition	|	UI hierarchy


--> Composition
    Composition = Compose's process of building and maintaining the UI structure described by composable functions.

    Example:
        @Composable
        fun App() {
            Text("Hello")
        }

    Compose Runtime works with the composable functions to create and maintain the UI.


--> 17. UI Displayed
    Finally the UI is displayed inside the Desktop window.

    Example:
        @Composable
        fun App() {
            Text("Hello")
        }

    Result:

        Window
        ┌───────────────────────────┐
        │ ProjectBasic01            │
        │                           │
        │ Hello                     │
        │                           │
        └───────────────────────────┘





















####################################################################################################################################
############################################ MODULE 4 : PRACTICE MULTIPLATFORM APPLICATIO ############################################

****************************************************** Chapter 1 - Creating the Project ***********************************************************************

----------------------- Problem : Gradle Import Failed -----------------------

--> Generate the Project
		The official Kotlin Multiplatform Project Generator creates a complete Gradle project.
		With this property :
			Project Name :	ComposeLearning_05
			Project ID   :	com.atek.composelearning


--> Open the Project
		Open Project
				↓
		IntelliJ detects settings.gradle.kts
				↓
		IntelliJ recognizes it as a Gradle project.
				↓
		IntelliJ starts Gradle Import.

--> Gradle Sync & AGP Compatibility & Build Errors

	Gradle is a Java application.Therefore Gradle requires a JVM to start.
    IntelliJ provides the configured Gradle JVM during Gradle Import.

	--> While importing we get version issue 

		Android Gradle Plugin (AGP)
			Purpose :	AGP extends Gradle with Android build support.

			Without AGP :	Gradle can build only normal Java/Kotlin applications.

			With AGP :
				Gradle can:
					• Build Android Applications
					• Read AndroidManifest.xml
					• Process Android Resources
					• Generate APK / AAB
					• Use Android SDK


	--> Problem

		Generated Project	:	 AGP Version = 9.0.1

		IntelliJ have Android Plugin	:	Supports only up to AGP 9.0.0-alpha06

		Result :	
				Version Mismatch ,Gradle Import Failed.
				IntelliJ cannot understand the requested AGP version.Gradle Import stops and the project is not imported completely.

		In our case : we update the intellij now intellij support our project AGP version
		
	Components
		IntelliJ IDEA	:	IDE
		Gradle	:	Build Tool
		AGP (Android Gradle Plugin)	:	 Adds Android build support to Gradle.
		Android SDK	:	Provides Android APIs, Build Tools and Platform Tools used by AGP.	
		

----------------------- Desktop Run Configuration -----------------------

After successful Gradle Import :

    See Gradle Section : 
        Gradle
            ↓
        desktopApp
            ↓
        Tasks
            ↓
        compose desktop
            ↓
        run

    Double-click :	run

    Result :  Desktop application starts on Windows.

    Note :
        Android Run Configuration is created automatically.

        Desktop application can be started using the Gradle  "compose desktop → run" task.

--> JDK[Java Development Kit] Selection
	 Provides the tools required to develop Java/Kotlin applications.Contain :   javac (Java Compiler)	|	java (JVM)	|	Development Tools	|	Standard Libraries

--> JDK used in our Project
	There are three places where JDK is involved.
		
		1. Project SDK
			Purpose :	Used by IntelliJ IDEA for the project.
			Location :	File	->	Project Structure	->	Project	->	Project SDK 
			

		2. Gradle JVM
			Purpose :	Used by Gradle during Gradle Import and Build.
			Location :	Settings	->		Build, Execution, Deployment	->		Build Tools		->		Gradle		->		Gradle JVM
		
		3. jvmToolchain()
			Purpose :	Specifies which JDK Gradle should use to compile the project.
			Example :
				kotlin {
					jvm {
						jvmToolchain(11)
					}
				}
				
--> Android SDK Detection 

	Purpose :	Provides the tools required to build and run Android applications.
	Contains :	Android APIs	|	Build Tools	|	Platform Tools	|	Emulator	|	ADB (Android Debug Bridge)

	--> Why do we need Android SDK?
		Our project contains an Android module (androidApp).
		During Android Build,
			Gradle
				↓
			Android Gradle Plugin (AGP)
				↓
			Android SDK
				↓
			APK Generated
		Without Android SDK,

			• Android code cannot be compiled.
			• APK cannot be generated.
			• Application cannot run on Android devices.

	--> How does IntelliJ detect Android SDK?
			When the project is opened,
			IntelliJ
				↓
			Android Plugin
				↓
			Search Android SDK

		If found, Android project is configured automatically.
		Otherwise, IntelliJ asks the developer to configure the SDK location.

	--> SDK Location

		Android SDK location is stored inside :   local.properties
			Example : sdk.dir=C:\\Users\\<UserName>\\AppData\\Local\\Android\\Sdk

		Purpose :  Tell Gradle where Android SDK is installed.

	--> SDK Manager
			SDK Manager is responsible for	: Downloading Android SDK	|	Installing API Levels	|	Installing Build Tools	|	Installing Platform Tools	|	Updating SDK Components

	--> Relationship
			IntelliJ IDEA
					↓
			Android Plugin
					↓
			Android Gradle Plugin (AGP)
					↓
			Android SDK
					↓
			Build Android Application

************************************************ Chapter 2 - Understanding the Entry Points ************************************************

--> Entry Point
		Entry Point is the first function executed when an application starts.
		Every platform has its own Entry Point.

--> Desktop Entry Point
		Location :	desktopApp	->	src	->	jvmMain	->	kotlin	->	Main.kt
		Example  :
			fun main() = application {

			}
		Purpose :	main() is the Entry Point of the Desktop application.

	--> Desktop Startup Flow
			User clicks Run	->	JVM Starts	->	main()	->	application()	->	Window()	->	App()

	--> main()
			Purpose 		:	Entry Point of the Desktop application.
			Responsibility  :	Starts Compose Desktop.

	--> application()
			Purpose 		:	Starts the Compose Runtime.
			Responsibility  :	Creates the application lifecycle.Manages application windows.

	--> Window()
			Purpose 		:	Creates a Desktop Window.
			Responsibility  :	Display the application's UI.

--> Android Entry Point 
		Location : 	androidApp	->	src	->	androidMain	->	kotlin	->	MainActivity.kt	
		Purpose  :	MainActivity is the Entry Point of the Android application.

	--> Android Startup Flow
		User taps App Icon	->	Android Operating System	->	MainActivity	->	onCreate()	->	setContent()	->	App()

	--> MainActivity
		Purpose :	Entry Point of the Android application.

	--> onCreate()
		Purpose :	First lifecycle function executed when Activity is created.

	--> setContent()
		Purpose :	Tell Compose what UI should be displayed.

--> Shared UI 
		Location :	shared	->	src	->	commonMain	->	kotlin	->	App.kt


	--> App()
			App() is the Root Composable of our application.It contains the UI shared by every platform.

	--> Why App() is inside shared?
			Both Desktop and Android display the same UI.
			Therefore App() is written once inside shared.

					App()
					   │
				┌──────┴──────┐
				│             │
			Desktop      Android

--> Separation of Responsibility

    desktopApp	Responsible for :	Starting Desktop Application.

    androidApp	Responsible for :	Starting Android Application.

    shared	    Responsible for :	Shared UI	|	Business Logic	|	Models	|	Utilities

--> libs.versions.toml
		Location :	gradle/libs.versions.toml
		Purpose  :
			Central place to manage :	Plugin Versions	|	Library Versions	|	Dependency Versions

	Instead of writing versions inside every build.gradle.kts, all versions are stored in one file.
	
--> Version Catalog
		Version Catalog is Gradle's modern dependency management system.

	Old Style :
		build.gradle.kts
			version("2.1.0")

	New Style :
		libs.versions.toml
				↓
			Alias
				↓
		build.gradle.kts

	Benefits :	One place for versions	|	Easier upgrades	|	Less duplication
	
************************************************  Chapter 5 - Build the First Screen ************************************************
--> Text()
	It is a Composable Function provided by the Compose library.

	Purpose : Display text on the screen.
	Syntax 	:
		Text(
			text = "Hello Kotlin"
		)
	Return Type	:	Unit
	
	Note :
		Text() does not create a window. It only describes the text that Compose should display.
		Can be called only from another @Composable function.
	
	
--> Modifier 	
	Purpose :	Controls how a Composable is displayed.
	Difference :
		Text()         → What to display.
		Modifier       → How to display it.
		
	--> Empty Modifier

		modifier = Modifier

		Meaning :	No additional behavior is applied.
		
	--> fillMaxWidth()

		Modifier.fillMaxWidth()

		Purpose :	Makes the Composable occupy the available width.

	-->	fillMaxSize()	

		Modifier.fillMaxSize()

		Purpose :	Makes the Composable occupy the available width and height.

	-->	Modifier Chain

		Multiple Modifiers can be combined.

		Example :	Modifier.fillMaxWidth().fillMaxSize()

	Note :
		Modifier changes the Composable's layout or behavior.
		It does NOT change what the Composable displays.
		
--> MaterialTheme
	
	Purpose :	Provides a common design (Theme) to all Composable functions.
	A Theme contains :	Colors	|	Typography	|	Shapes
	
	--> Why do we use MaterialTheme?
		Instead of configuring every Composable separately, MaterialTheme provides a common design that is shared by all child Composables.
		
		Example :
			MaterialTheme {
				Text("Hello Kotlin")
			}
	
	Production Project Example :
		AtekTheme {
            App()
        }
	
	Note :
		MaterialTheme does not display anything on the screen.
		It only provides design information to its child Composables.
		
--> Column() 
	Purpose :	Arrange child Composables vertically.
	Type 	:	Layout Composable.

	Syntax
		Column {
			Text("Hello")
			Text("Kotlin")
			Text("Compose")
		}

	Result	:
		Hello
		Kotlin
		Compose

	UI Composables	Example 	: Text()	|	Button()	|	Image()
	Layout Composables Example  : Column()	|	Row()		|	Box()

	Note :
		Column() does not display any UI.
		It only arranges child Composables vertically.
		
--> Button() 
	Purpose :	Display a clickable button.
	Type 	:	UI Composable.

	Syntax	:	
		Button(onClick = {	}) {
			Text("Click Me")
		}

	-->	onClick
		Executed when the user clicks the Button.

	-->	Content	
		The second lambda defines the UI displayed inside the Button.

	--> Hierarchy
		Button
			│
			└── Text

	Note :
		Button is a clickable container.It can display any Composable as its content.
		Text() is commonly used as the button label.

		
--> Row()

	Purpose :	Arrange child Composables horizontally.
	Type 	:	Layout Composable.

	Syntax	:	
		Row {
			Text("Hello")
			Button(
				onClick = { }
			) {
				Text("Click Me")
			}
		}

	Result	:	
		Hello    [ Click Me ]

	Note :
		Like Column(), Row() does not display UI.It only arranges its direct child Composables.
		
--> Box() 

	Purpose :	Arrange child Composables by stacking them on top of each other.
	Type 	:	Layout Composable.

	Syntax	:	
		Box {
			Text("Hello")
			Button(
				onClick = { }
			) {
				Text("Click Me")
			}
		}

	Result	:	Child Composables are displayed in the same space (Overlay).

	Common Uses	:	Image + Text	|	Loading Screen	|	Notification Badge	|	Floating Button	|	Overlay UI

	Note :	Box() does not display UI. It only arranges its direct child Composables.
		
	Difference
		Column()	:	Arranges children vertically.
		Row()		:	Arranges children horizontally.
		Box()		:	Stacks children on top of each other.


************************************************ Chapter 6 - State Management ************************************************

--> State :
	Data that can change while the application is running.
	Example :	Counter Value	|	Username	Password	|	Login Status	|	Selected Item	|	Dark Mode
	
--> remember()

	Purpose :	Store a value in Compose memory.
	Syntax	:	
		remember {
			Initial Value
		}

	Example	:
		val count = remember { 0 }

	Problem Solved :
		Prevents values from being recreated during Recomposition.

	Limitation
		remember() does NOT notify Compose when the value changes.

	Note	:	remember() remembers a value.  It does not make the value observable.
	
--> mutableStateOf() 
	Purpose 		:	Create an observable State object.
	Syntax			:	mutableStateOf(0)

	Return Type		:	MutableState<T>
	
	Example			:	val count = mutableStateOf(0)

	Read Value		:	count.value

	Update Value	:	count.value++

	Responsibilities :	Store the value.	|	Allow the value to change.	|	Notify Compose when the value changes.

	Difference		:
		remember()			:	Keeps the State object alive.
		mutableStateOf()	:	Makes the value observable.

	Flow	:
		count.value changes		->	 Compose notified	->	Recomposition	->		UI Updated
		
--> by (Property Delegation)
	
	Purpose :	Remove the need to use .value when working with MutableState.

	Without by	:	
		var count = remember {
			mutableStateOf(0)
		}

		Read 	:	count.value
		Update 	:	count.value++

	With by	:
		var count by remember {
			mutableStateOf(0)
		}

		Read 	:	count
		Update	:	count++


	Required Import:
		import androidx.compose.runtime.getValue
		import androidx.compose.runtime.setValue

	Note :
		by delegates reading and writing to the underlying MutableState object.
		It is Kotlin's Property Delegation feature, not a Compose feature.
		
--> Recomposition

Purpose :	Re-execute Composable functions when State changes.

Flow	:	State Changes	->	Compose detects the change	->	Recomposition starts	->	Affected Composable functions execute again		->		UI is updated

Example	:	
		count++
            ↓
    mutableStateOf detects the change
            ↓
    App() executes again
            ↓
    Text("Count : $count")
            ↓
    Screen updates

Note :
    During Recomposition,
        • State values are preserved by remember().
        • Only Composable functions depending on the changed State are recomposed.
        • The entire application is not recreated.
		
************************************************ Chapter 7 - Modifier & Layout Mastery ************************************************

--> Modifier Chain
	
	Purpose :	Apply multiple Modifiers to a Composable.

	Example	:	Modifier.fillMaxWidth().background(Color.Yellow).padding(16.dp)

	Execution Order	:	Modifier	->	fillMaxWidth()	->	background()	->	padding()

	Note :	
		• Each Modifier receives the result of the previous Modifier.
		• Modifiers are applied in the order they are written.
		• Changing the order can produce a different UI.
		
--> padding()

	Purpose :	Adds spacing around a Composable.
	Syntax	:	Modifier.padding(16.dp)

	Example	:
		Modifier
			.background(Color.Red)
			.padding(16.dp)

	Behavior :
		background()
			↓
		Draws the background.

		padding()
			↓
		Adds spacing around the previous result.in between text and background

	Example
		background()
			↓
		padding()

		Result :	Red background with spacing around the Text.

	Note
		• padding() creates spacing.
		• It does not change the Text size.
		• Modifier order affects the final UI.
		
--> size() 

	Purpose :	Sets both Width and Height of a Composable.
	Syntax	:	Modifier.size(100.dp)
	Example	:
		Box(
			modifier = Modifier
				.size(100.dp)
				.background(Color.Red)
		)

	Note
		• size() changes the Composable size.
		• It does not change the content size.
		• Width and Height become equal.
		
--> width()
	
	Purpose	:	Sets only the Width.
	Syntax	:	Modifier.width(200.dp)
	Example	:
		Text(
			text = "Hello Kotlin",
			modifier = Modifier
				.width(200.dp)
				.background(Color.Red)
		)
	
	Note
		• Height remains unchanged.
		• Only Width changes.
		
--> height ()
	
	Purpose	:	Sets only the Height.
	Syntax	:	Modifier.height(100.dp)
	Example	:
		Text(
			text = "Hello Kotlin",
			modifier = Modifier
				.height(100.dp)
				.background(Color.Red)
		)
	
	Note
		• Width remains unchanged.
		• Only Height changes.	
		
--> fillMaxWidth()
	
	Purose : 	Occupies the maximum available width provided by the parent.
	Syntax :	Modifier.fillMaxWidth()
	Example:
		Text(
			text = "Hello Kotlin",
			modifier = Modifier
				.fillMaxWidth()
				.background(Color.Red)
		)
	Note
		• Width depends on Parent.
		• Height remains unchanged.
		
--> fillMaxSize()

	Purose : 	Occupies the maximum available Width and Height provided by the parent.
	Syntax :	Modifier.fillMaxSize()
	Example:
		Box(
			modifier = Modifier
				.fillMaxSize()
				.background(Color.Red)
		)
	Note	:
		• Depends on Parent size.
		• Content size does not change.
		• Composable occupies the available area.
		
--> Alignment 

	Purpose :	Aligns children on the Cross Axis.

	Column	:
		horizontalAlignment	:	Start	|	CenterHorizontally	|	End

	Row		:
		verticalAlignment	:	Top		|	CenterVertically	|	Bottom

	Note	:
		• Alignment works on the Cross Axis.<---------->
		• Column → Horizontal Alignment
		• Row → Vertical Alignment
	
--> Arrangement

Purpose :	Positions children on the Main Axis.

Column	:	
    verticalArrangement	:	Top	|	Center	|	Bottom	|	SpaceBetween	|	SpaceAround		|	SpaceEvenly

Row		:
    horizontalArrangement	:	Start	|	Center	|	End	|	SpaceBetween	|	SpaceAround		|	SpaceEvenly

--> Difference
		Alignment	:	Controls Cross Axis.
		Arrangement	:	Controls Main Axis.

	Note
		• Column → verticalArrangement
		• Row → horizontalArrangement

--> Nested Column & Row
	Example :
		Column {
			Text("Title")
			Column {
			Text("Name")
			Text("Age")
			}
		}
		Row {
			Text("Name")
			Row {
				Button(...) { }
				Button(...) { }
			}
		}

--> Box 
	Example :
		Box(
			modifier = Modifier.fillMaxSize(),
			contentAlignment = Alignment.Center
		) {
			Text("Hello")

		}